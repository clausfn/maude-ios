# Requirements Trace Matrix (RTM)

_QMS-lite spine. One row per requirement: SRS FR-/NFR- ID → design element → test ID → status. Kept at IEC 62304 Class B rigour. Version: 2026-08-13 (rows PR-112 — donor programme DON-2026-01 app side, FR-DON-01…06 / T-DON-01…06, filed with a RISK section; see the dated block before "Upcoming". Previous: row PR-110 — A7.2 design-QA sweep fixes, batch "electric-ink Q3": FR-PROV-02 copy rule + T-DED-06, NFR-UI-THEME-01 primary-button + deviation-outlier tokens, NEW NFR-UI-EDGE-02 horizontal-strip trailing inset, UC-01 off-path step counter. No safety-path behaviour changed — no RISK touch). Previous: 2026-08-12 (A7.2 full-screen program, row PR-107: 22 `[PROV — SRS v06 pending]` rows appended from the 74-screen census + SRS v06 diff proposal; see the dated block before "Upcoming". Previous: 2026-07-03 — launch audit: T-PROV-01 record corrected; 8 missing rows added — FR-RSCH-03, FR-WAL-08, FR-PROV-02, FR-PAT-01/02, FR-PAS-03, FR-ING-08, NFR-RSCH-04. Prior: 2026-06-03)._

Status legend: `planned` · `in-progress` · `implemented` · `verified`.

| Req ID | Title | Design element (file) | Test ID | Status | PR |
|---|---|---|---|---|---|
| D-STORE | Deployment-agnostic signing; account swap = config change | `Config/Signing.xcconfig`, `Config/Signing.example.xcconfig`, `Liviqa.xcodeproj/project.pbxproj`, `docs/SIGNING.md` | T-SIGN-01 | verified | PR-1 |
| FR-ARCH-04 | Read-only HealthKit (never write) | `Liviqa/Liviqa.entitlements` (HK enabled), `Info.plist` (NSHealthShareUsageDescription); read-only enforced in code | T-HK-RO-01 | in-progress (entitlement + usage string set; empty write set enforced in `HealthKitService`, PR-4) | PR-1 |
| FR-QMS-01 | Intended-use statement under version control | `docs/INTENDED_USE.md` | (review) | implemented | PR-30 |
| Release / TestFlight | Shipped build feeds LIVE data (Release→`sovereignProd`: `api.liviqa.app` + Supabase GoTrue); Debug→`.mock`; env override; video consult gated off v1 | `Liviqa/Config.swift` (`backend`, `sovereignProd`, `videoConsultEnabled`), `Liviqa/Views/MessagesView.swift` | build verified (iOS Sim); on-device archive pending DfG signing | implemented | PR-30 |
| App Store compliance | Privacy manifest + export-compliance + DfG team/bundle | `Liviqa/PrivacyInfo.xcprivacy`, `Liviqa/Info.plist` (`ITSAppUsesNonExemptEncryption`), `Config/Signing.xcconfig` (team `PS258XSNL8`, bundle `app.liviqa.ios`) | manifest bundled (build verified); App ID/cert/App-Store-Connect = Apple-side blockers | in-progress | PR-30 |
| FR-QMS-02 | Living ISO 14971 risk file | `qms/RISK.md` | — | in-progress | PR-1 |
| FR-QMS-03 | Requirement ↔ test traceability for all FR/NFR/DM | `qms/RTM.md` (this file) | — | in-progress | PR-1 |
| FR-QMS-05 | Phase-2 docs + changelogs as DHF skeleton | `qms/DHF.md`, `qms/CHANGELOG.md`, `qms/VnV.md` | — | in-progress | PR-1 |
| DataModel v1 §2.1 | Typed sample sources (12 entities) carry source/tier/provenance | `Liviqa/Models/Domain/Entities.swift` | T-DM-03 | implemented (tests authored; run in Xcode) | PR-2 |
| DataModel v1 §2.1 | Clinical-tier rejects SIMULATED (schema gate + type-level `ClinicalProvenance`) | `Liviqa/Models/Domain/CoreTypes.swift` | T-DM-01 | implemented | PR-2 |
| OD-07 / FR-ING-06 | Glucose canonical mmol/L (GMI = HbA1c headline) | `CoreTypes.swift` (`GlucoseUnit`), `GlucoseSample.mmol` | T-DM-02 | implemented | PR-2 |
| OD-09 | On-device SwiftData store, file-protection complete | `Liviqa/Models/Domain/Persistence.swift` | T-DM-03 | implemented (store FP-complete; DEK key-wrap = `Security/`) | PR-2/PR-8 |
| NFR-PRIV-01 | Samples never sync to cloud (`cloudKitDatabase: .none`) | `Persistence.swift` | — | implemented | PR-2 |
| FR-ING-01 | MVP read set (HRV-SDNN, RHR, steps, active energy, glucose, sleep, workouts) modelled as value readings | `Liviqa/Ingestion/HealthSamples.swift` | T-ING-01, T-ING-02 | implemented | PR-3 |
| FR-ING-07 | Single `HealthSamples` aggregate; no network-upload method on provider/types | `HealthSamples.swift`, `HealthDataProvider.swift` | T-ING-01 | implemented | PR-3 |
| FR-ARCH-04 | Read-only: HealthKit share/write set is **empty**; no write method anywhere | `Liviqa/Ingestion/HealthKitService.swift` (`shareTypes = []`), `HealthDataProvider.swift` | T-HK-RO-01 | implemented (typechecks vs HK SDK; unit run in Xcode) | PR-4 |
| FR-SHARE-01 | Derive scoped share package for `PUT /shares/{grantId}` (consent GROUPS; derived-only; no provenance/source) | `Liviqa/Sharing/DerivedShareBuilder.swift` | `DerivedShareBuilderTests` (Xcode) | implemented (canonical builder; per backend contract §4) | (existing) |
| FR-SHARE-02 / FR-WAL-01,05,07 | Wire device→sovereign backend: `Config.backend` selector, `LiviqaBackendService` (GET /me·/recipients·/grants·/ledger, POST /grants[/revoke]), `pushDerivedShare → PUT /shares/{grantId}` | `Config.swift`, `Liviqa/Services/LiviqaBackendService.swift`, `Liviqa/Services/BackendMapping.swift`, `AppState.createGrantAndShare`/`pushDerivedShare` | T-BMAP-01..06; live curl loop (POST /grants 201 · PUT /shares 200 · revoke ok) | implemented (mapping verified swiftc; write loop verified against localhost:3001) | PR-12 |
| FR-SHARE-02 (UI) / UC-07,11,12 | Citizen share flow: live recipient picker (`GET /recipients`), group-scope selection, create grant + push derived package, stop via revoke; renders live grants + ledger. A7.2 Area ⑥: wizard → single-scroll form with real preview phase (summaries-only — the canvas's "Every reading" mode NOT built, RK-SHARE-03); UC-11 recipient-type chooser added | `Liviqa/Views/ShareWithClinicianView.swift` (v02 single-scroll + `roleFilter`), `Liviqa/Views/CreateGrantView.swift` (new, UC-11), `Liviqa/Views/WalletView.swift` | sourcekit-clean; dd6 sim build green 2026-08-12 | implemented | PR-13 → row PR-107 |
| NFR-SEC-07 | Cloud holding real PII must be EU-sovereign, not US-parented (Supabase → `liviqa-backend`, Scaleway+Ory) | `Config.Backend` (`.mock` default · `.supabaseSandbox` · `.sovereign`); `LiviqaBackendService` | (covered by FR-SHARE-02 tests) | implemented (sovereign client live; Supabase demoted to sandbox; default `.mock`) | PR-12 |
| NFR-SEC-07 (auth) / FR-WAL-* | Real identity via Ory Network (native login → session token presented as Bearer; backend validates) | `Liviqa/Services/OryAuthClient.swift`; `LiviqaBackendService` (Ory-aware auth); `Config.oryURL`/`.sovereignStaging` | T-ORY-01..03 (parsers, swiftc-verified) | implemented (parsers verified; live login pending on-device — Ory egress blocked in CI sandbox) | PR-14 |
| NFR-SEC-01 / FR-AUTH-03 | Auth token in Keychain (Ory session token persisted device-only; restore on launch, clear on sign-out) | `Liviqa/Security/SessionTokenStore.swift`; `LiviqaBackendService` init/sign-in/sign-out | (swiftc typecheck; on-device Keychain pending) | implemented | PR-18 |
| UC-consult / video + messaging | Citizen care-team surface: notifications, joinable consults (Jitsi room), citizen-authoritative recording consent, secure messaging threads | `Liviqa/Services/CareConnect.swift`, `LiviqaBackendService` (CareConnect), `AppState.refreshCareInbox`; contract `docs/Video_and_OAuth_Contract_v01.md`; backend `citizen.controller`/`citizen.service` (`GET /threads`, `GET/POST /threads/:recipientId/messages`) | backend `citizen.service.spec.ts`; live curl (list/read/send, 403 on no grant) — DTOs match `ThreadDTO`/`MessageDTO` | implemented (backend routes live, liviqa-backend PR-20) | PR-18 |
| UC-consult (UI) | Care tab: messaging chat + consult join (EU Jitsi WebView) + citizen recording-consent toggle | `Liviqa/Views/MessagesView.swift`, `Liviqa/Views/ConsultView.swift`, `Liviqa/Views/MainTabView.swift` (Care tab) | app builds for iOS Simulator (Xcode 26) | implemented (UI) | PR-19 |
| FR-AUTH-01 / NFR-SEC-01 (auth) | Auth via **Supabase Auth (self-hosted GoTrue, EU)** — email/password + Sign in with Apple (`ASAuthorizationController`, SHA-256 nonce → GoTrue `id_token` grant); Supabase access-token JWT presented as the backend bearer + Keychained. (Reverted from Ory: custom-domain cost.) | `Liviqa/Services/SupabaseAuthClient.swift`, `AppleSignInCoordinator.swift`, `AuthView.appleSignIn`, `AppState.signInWithApple`, `LiviqaBackendService` (auth), `Config.supabaseAuthURL` | T-SBA-01..05 + nonce (`SupabaseAuthParsingTests`) — 8-suite run green in iOS Simulator | implemented (GoTrue self-host = deploy step; sheet needs SIWA entitlement on device) | PR-22/26 → PR-28 |
| FR-ING-03 / FR-ING-04 | Anchored incremental HealthKit reads (`HKAnchoredObjectQuery` + persisted `HKQueryAnchor`), observer queries + per-type background delivery; anchors stored ENCRYPTED on-device (KeyVault DEK), keyed to a device-local user (never UserDefaults, never a server id) | `Liviqa/Ingestion/HealthKitService.swift`, `Liviqa/Ingestion/HealthKitService+Anchored.swift` (`IncrementalHealthSource`, `AnchorCodec`), `Liviqa/Security/EncryptedAnchorStore.swift` (+ `LocalUserScope`) | T-ANCH-01..06 (`EncryptedAnchorStoreTests`) + T-ANCH-07 (`AnchorCodecTests`) + T-ANCH-08 (fake-provider anchor-advances-on-sync via `AnchorSync`) — iOS Simulator | implemented (codec/store/advance tested; observer+background-delivery run on device). Cadence: steps `.hourly`, others `.immediate`; re-entrancy guard coalesces overlapping syncs | PR-24/PR-27 |
| NFR-SEC-07 (video) | Video stays EU-sovereign: `Config.jitsiDomain = nil` (secure-shell default); `jitsiDemoDomain = "meet.jit.si"` is LOCAL-parity only (US-operated, never production); TODO to set the real EU domain | `Liviqa/Config.swift` | (build-time constant; secure-shell fallback in `ConsultView`) | implemented | PR-25 |
| UC-13 / SRS §5.4 (CE) | Consent ledger surfaced from append-only `consent_event` | `LiviqaBackendService.fetchEvents → GET /ledger` | T-BMAP-02/03 | implemented | PR-12 |
| FR-PROV-01 | Per-metric source arbitration: highest tier wins, lower fills gaps, never blend clinical with estimate | `Liviqa/Ingestion/SourceArbiter.swift`; `AppState.refreshFromHealth()` | T-ARB-01..05 | implemented (verified via swiftc) | — |
| FR-ING-02..05 | Concrete HealthKit reads (glucose mmol/L, HRV/RHR, steps, energy, sleep, workouts) | `HealthKitService.swift` | T-HK-RO-02/03 | implemented | PR-4 |
| FR-ING-09 | L1→L2 normalization + idempotent persistence into SwiftData | `Liviqa/Ingestion/IngestionCoordinator.swift` (`SampleMapper`) | T-MAP-01, T-MAP-02 | implemented (run in Xcode) | PR-4 |
| FR-NDG-01..05 | On-device heuristic nudge engine; baseline-relative; capped allow-list output | `Liviqa/Intelligence/NudgeEngine.swift`, `NudgeModel.swift` | T-NDG-01/04/05/07 | implemented (compiled + executed via swiftc) | PR-5 |
| FR-NDG-06 | Forbidden-construction guard (no dose/diagnosis/normality), **BLOCKING** | `Liviqa/Intelligence/NudgeGuard.swift` | T-NDG-06/06b/06c | **implemented & passing** (executed this session) | PR-5 |
| FR-NDG-02 §6.2 | Workout↔glucose coupling rule (lead example): within-user post-session glucose drop vs own prior same-type sessions; guard-clean | `Liviqa/Intelligence/NudgeEngine.swift` (`workoutGlucoseNudge`) | T-NDG-08 (`WorkoutGlucoseNudgeTests`) | implemented (executed via swiftc) | PR-15 |
| FR-PAS-05 / DM-05 | On-device derived Passport stats (readings, days tracked, TIR, avg sleep) from local samples; composed with count half in app state | `Liviqa/Intelligence/PassportStatsDeriver.swift`, `Liviqa/Models/PassportStats+Derived.swift`, `AppState.refreshFromHealth` | T-PAS-01 (`PassportStatsDeriverTests`) | implemented (deriver executed via swiftc; UI wiring Xcode-pending) | PR-16 |
| FR-PAS-05 / DM-06 | On-device 7-day correlation grid (deviation-from-baseline heat map) + pattern note/sources/strength; external signals honestly noData | `Liviqa/Intelligence/CorrelationDeriver.swift`, `Liviqa/Models/CorrelationWeek+Derived.swift`, `AppState.refreshFromHealth`; **grid UI** `Liviqa/Views/HealthPassportView.swift` (`correlationCard`, consumes `appState.correlationWeek`; per-cell a11y labels, NFR-A11Y-01; no trust chips) | T-COR-01 (`CorrelationDeriverTests`) | implemented (grid view now built, PR-23; app builds for iOS Simulator) | PR-17/PR-23 |
| D9 / FR-REG-03 | AFib/cardiac lane is display-only: route-to-clinician, no interpretation | `NudgeEngine.afibNudge`, `RegulatoryLane.displayOnly` | T-NDG-02, T-NDG-03 | implemented | PR-5 |
| L3 lane map | Each stream tagged with its regulatory lane | `RegulatoryLane` (wellness/watch/constrained/displayOnly) | T-NDG-04 | implemented | PR-5 |
| FR-CTX-01 | Open-Meteo weather + AQI; coarse (~0.1°) coordinate, per-session, no health egress | `Liviqa/Context/OpenMeteoWeatherProvider.swift`, `WeatherContext.swift` | T-CTX-01/02/03 | implemented (executed via swiftc) | PR-6 |
| FR-CTX-02 | Weather snapshot persists as `WeatherContext`, provenance EXTERNAL | `Liviqa/Context/WeatherContextMapper.swift` | T-CTX-04 | implemented (run in Xcode) | PR-6 |
| FR-ARCH-05 | "Demo data" indicator source flag (mock = demo) + UI badge | `HealthDataProvider.swift` (`DataProviderKind.isDemoData`); `AppState.isDemoData`; `TodayView` badge | T-ING-05 | implemented (data flag + UI badge) | PR-7 |
| INT-L1L3 | Live L1→L2→L3 feed wired to Today screen | `AppState.refreshFromHealth()`; `EngineNudge+Card.swift`; `MainTabView` | (UI; user-verified) | implemented | PR-7 |
| NFR-PORT-01 | L1 ingestion portable (framework-free value types, provider protocol) | `Liviqa/Ingestion/*` | T-ING-01..05 | implemented | PR-3 |
| NFR-PRIV-05 / build-rule | `provenance` never renders (file guard + type guard, **blocking**) | `scripts/guard_provenance.sh`, `build-ios.sh` (blocking step), `scripts/archive_upload.sh` (blocking PRE-ARCHIVE step, added 2026-08-13), `ProvenanceGuardTests.swift` | T-PROV-01, T-PROV-02, T-PROV-03 | implemented. CORRECTION (launch audit 2026-07-03): "file guard green" recorded here since PR-3 was false — the guard was wired to no build/CI step (inert) and was exiting 1 on wallet-receipt word collisions. Fixed + wired 2026-07-03 (PR-102): pattern scoped to the provenance data field, guard now a blocking `build-ios.sh` step, green. Substantive never-renders rule held throughout (audit-verified); type tests run in Xcode. SECOND CORRECTION (2026-08-13): the RELEASE chain was weaker than the dev chain — `scripts/archive_upload.sh` ran only `guard_release_posture.sh` (on the exported .ipa, pre-upload) and never `guard_provenance.sh`, so a provenance regression could be archived and shipped while `build-ios.sh` stayed green. Now wired as a blocking PRE-ARCHIVE step (`set -e` aborts before anything is built). STILL OPEN: `scripts/archive_upload_isolated.sh` — the isolated-keychain chain actually used for headless TestFlight uploads — runs NEITHER guard | PR-3 → corrected PR-102 → release chain corrected 2026-08-13 |
| FR-SHARE-01 | Derive the scoped share package per consented group (derived-only, no provenance) → `PUT /shares` body | `Liviqa/Sharing/DerivedShareBuilder.swift` | T-SHARE-01..04 (`DerivedShareBuilderTests`) | implemented (run in Xcode); contract: `Liviqa_iOS_Backend_Contract_v01` | PR-TBD |

| NFR-UI-THEME-01 | Dual **Paper/Midnight** theme, runtime-switchable, **default Paper** (PR-47; was Midnight); all brand tokens dynamic (light=Paper, dark=Midnight), single root `preferredColorScheme` flip; no invented hues; status bar/tabs invert. **Design-QA 2026-08-13 (row PR-110):** two mode-split defects closed at token level — (a) `primaryFill`/`primaryLabel`/`primaryOffFill`/`primaryOffLabel` added so a primary action has ONE filled treatment per edition (teal+white by day, off-white+marine at night; a `moss`-filled primary was 2.46:1 on marine and read as disabled beside a cream `Done`), (b) `devOutlier` dark lifted from bright yellow 0xFFC53D to the SAME orange as Morning (0xF08C1E) so "worth noticing" carries equal severity in both editions. No new palette values: both primary values already existed (`moss` light / `invertBG` dark). `accentHeart`, the TIR ramp and `amber` untouched | `Liviqa/Theme.swift` (`Color.dyn`, `LiviqaTheme.Mode`, invert/hero/grid hooks, **primary\* + primaryGlow**), `Liviqa/LiviqaApp.swift`, `Assets.xcassets/LaunchBackground.colorset` | build (iOS Sim, both modes); Midnight screenshot verified; PR-110 evidence = `a72_designqa_20260813/share_phase_{preview,done}_midnight.png`, `share_receiptoffer_midnight.png`, `insights_bottom_{paper,midnight}.png` | implemented | PR-34 → PR-110 |
| NFR-UI-OURA-02 | "Oura pass" applied to all 5 screens (Today/Trends/Wallet/Care/Journal) — gradient rings + glow + draw-in, gradient-area charts, invert surfaces, pills, CE-ledger toast; brand/logo/copy/data-model unchanged; all surfaces legible on both themes (invert tokens, no `ink`-as-surface+white-text) | `OuraComponents.swift`, `TodayView`, `WeekInContextView`, `WalletView`, `MessagesView`, `JournalView`, `MainTabView` | build (iOS Sim); per-screen Midnight screenshots verified | implemented | PR-36…PR-40 |
| NFR-BRAND-LOGO-04 | Locked aperture mark always visible + correct variant per surface: reversed (white ring) on dark Midnight, primary (ink ring) on light Paper; asset never code-redrawn; no rotation/animation on the mark | `Liviqa/Views/Components.swift` (`LiviqaApertureMark` auto by `colorScheme`), AuthView/PrivacyDeclarationView/MessagesView call sites, `Assets.xcassets/LiviqaMark*.imageset` | build (iOS Sim); Today app-bar white-ring screenshot verified | implemented | PR-42 |
| NFR-UI-V2-05 | v2 redesign — calm wellness companion: 5-tab IA (Home·Insights·Journal·Privacy·Settings, Care off bar); Home leads with affirming state + wellness-pillar signals; Stress/Recovery a first-class descriptive pillar; assistant hand-off. Brand + two-state colour intact; descriptive-only (no scores/verdicts) | `MainTabView`, `TodayView`, `WeekInContextView`, `WalletView`(→Privacy), `ProfileSheet`, `NudgeDetailView` | build (iOS Sim); per-screen Midnight screenshots verified | implemented | PR-50…PR-53 |
| FB-CHAT-01 | Beta fix — care message thread must always show typed text and the latest message (tester: "classic chat issue, can't see the message"). Composer text never follows system label colour; scroll-to-latest on open/new/keyboard; interactive keyboard dismiss | `MessageThreadView` (MessagesView.swift), `MainTabView` (debug deep-link) | build (iOS Sim) ✓; thread screenshot verified; composer colour deterministic | implemented | PR-57 |
| FR-ING-DEFAULT-01 | Go-live: read REAL HealthKit by default on a Health-capable platform; fall back to demo on Simulator/no-Health/UI-test; "Demo data" flag accurate (real only after a non-empty fetch); never show fabricated nudges as the user's own | `AppState.resolveProviderKind`/`usingRealData`/`refreshFromHealth`, `HealthProviderFactory.isRealHealthDataAvailable` | build ✓; `ProviderResolutionTests` 6/6; 5-tab smoke screenshots | implemented | PR-58 |
| FR-EID-LOGIN-01 | Simulated identity-wallet login (eIDAS 2.0): AltID (Denmark's official EUDI wallet — unlock → selective disclosure with zero-knowledge proof, "Over 18" without DOB → verify → EUDI ref) e-Boks ID (proof-of-age wallet), and iGrant.io (EUDI Data Wallet, SD-JWT + consent receipt); + DfG Wallet = 4 identity options in a 2×2 grid. Brand-coloured per provider; records login provenance; high-fidelity for demos, no live IDP; descriptive (no "simulated" labels) | `IDProviderLoginView`, `IDProvider.altID/.eBoks`, `AuthView` (2-up row + DfG), `AppState.signInWithProvider`, `Config.nationalIDLoginEnabled` | build ✓; AltID/e-Boks screenshots + AuthView 3-option screenshot verified | implemented | PR-62 |
| NFR-VIZ-CLIN-01 | Per-metric clinical chart conventions: discrete days as bars (sleep, TIR — with 70% target rule), personal-baseline band + tight y-domain for RHR/HRV (never from zero), CGM day curve with meal excursions; demo seeds physiologically plausible + cross-metric coherent for the founder persona (TIR 50–75%, RHR 52–68, HRV 25–55, Deep 10–20%) | `DailyBarsChart`, `AreaTrendChart` (band), `MockDataProvider.fetchSamples`, `MetricDetailView.trendCard` | sim screenshots: Insights TIR 71% varied; glucose/heart/sleep details | implemented | PR-71 |
| FR-CARE-SCHED-01 | Care tab video surface lists upcoming SCHEDULED consultations only (clinician name + time, soonest first, server-filtered to scheduled+future); informative empty state; ended/old calls never listed (4h freshness window on active sessions, server-side) | `MessagesView.scheduledSection`, `CareConnect.fetchScheduledConsults`, backend `GET /appointments` + citizen `consults/active` freshness filter | Sim screenshots (populated + empty) vs local backend; live sandbox curl (book → citizen sees; past hidden) | implemented (sandbox) | PR-67 |
| FR-WALLET-UCA-01 | UC-A — citizen issues their own "Liviqa Citizen" sign-in credential into My DfG (pseudonymous: role/member-id/date; no name, no health data); REAL Partisia-sandbox issuance via the sovereign backend; QR (cross-device) + deep link (same-device); issuance visible in the Privacy Record (CE event) | `WalletView.citizenCredentialRow`, `ShareReceiptSheet` (.citizenCredential), `AppState/SovereignSharing.issueCitizenCredential`, backend `POST /issuance/citizen-credential` | live Sim run vs localhost backend → real haip-vci QR (screenshot); backend 201 curl | implemented (sandbox) | PR-64 |
| FR-WALLET-LOGIN-01 | DfG Wallet login: in-app simulation of the eIDAS 2.0 identity use cases — unlock → selective disclosure (prove facts, hide name/DOB/address) → Partisia verification (MPC + CE-ledger anchoring) → "Verified with Partisia" + CE ref → signed in. High-fidelity for demos/pilots; no live Partisia backend; descriptive (no on-screen "simulated" labels) | `DfGWalletLoginView`, `AuthView` (Continue with DfG Wallet), `AppState.signInWithDfGWallet`, `SettingsView` (verified badge), `Config.dfgWalletLoginEnabled` | build ✓; 4 steps screenshot-verified + live tap-through (auto-verify → CE ref → enter) | implemented | PR-61 |
| FR-SLEEP-01 | Real sleep visualisation: last-night Deep/Light/REM breakdown (minutes + %), asleep total, nightly-hours week — from on-device HealthKit, descriptive-only (no score/duration verdict); demo fallback | `SleepDeriver`, `AppState.sleepSummary`, `MetricDetailView` (sleep) | build ✓; Sim screenshot shows derived stages (17/64/19%); `SleepDeriverTests` 5/5 | implemented — SUPERSEDED in part by A7.2 Area ④: `SleepDetailView` + `SleepDetailDeriver` add the DSleep verdict + a TRANSPARENT decomposed score (rest/depth/rhythm, FR-TOD-05 arithmetic stance; RISK.md Area ④ entry). The "no score" clause of this row is deliberately relaxed there; descriptive-only framing otherwise intact (`SleepDetailDeriverTests`) | PR-60 |
| FR-REALDATA-01 | All viz surfaces show the user's REAL derived data (Home chips+sparklines, Insights cards+heatmap, all pillar details) with honest demo fallback; fabricated deltas hidden next to real values; app-level one-time ingest + reentrancy guard | `TodaySignals(+glucoseToday)`, `TodaySignalsDeriver`, `MetricDetailView`, `WeekInContextView`, `MainTabView`, `AppState` | build ✓; Home/Insights/glucose/recovery/sleep screenshots verified real-derived; 114 tests; 5-tab smoke | implemented | PR-60 |
| NFR-VIZ-01 | Data visualization lifted to Oura/Whoop caliber within the locked brand, descriptive-only: smoothed trend charts with a y-axis scale and a personal "your normal" band (mean ±1σ of the user's OWN data, never a clinical range); Home chip 7-day sparklines from real per-day series (no fabricated zeros); CGM-style daily glucose curve inside the personal target band | `OuraComponents` (AreaTrendChart, MiniSparkline, GlucoseCurveView), `TodaySignals`/`TodaySignalsDeriver` (week series), `TodayView`, `MetricDetailView`, `WeekInContextView` | build ✓; Insights/Home/glucose/sleep screenshots verified; +3 series tests (109 total) | implemented | PR-59 |
| NFR-VIZ-DAY-01 | **Day-axis integrity (2026-08-13, latent defect closed).** Any chart with a day axis must place each value on its OWN calendar day and derive its labels from those dates. A daily series holds one entry per day WITH data, so its index is not a day number — drawing it at evenly spaced x-positions against separately computed day labels mis-labelled every value after a gap (RK-CHART-01 in `/qms/RISK.md`). A day with no reading renders as a GAP: no bar, no dot, no line drawn through it — never a zero, never an interpolated segment. A dateless series is placed only when it covers the window 1:1; otherwise the day labels are dropped rather than guessed | `Liviqa/Intelligence/DaySeries.swift` (new — `DaySlot`, `slots`, `aligned`, `runs`; pure Foundation, NFR-PORT-01), `TrendsDeriver` (`tirDailyDates`/`hrvDailyDates`, `windowStart`/`windowEnd`, `tirSlots`/`hrvSlots`), `TodaySignalsDeriver`/`TodaySignals` (per-series dates + `slots(for:over:)`), `TrendsCharts.TIRTrendBarChart` (slot-driven bars + date-derived edge labels), `OuraComponents.AreaTrendChart` (`daySlots`, per-run paths, ticks positioned on the point x-map), `OuraComponents.MonthTrendLine` (`slots`), `WeekInContextView`, `TrendsView`, `TodayView` | `LiviqaTests/DayAxisAlignmentTests` (14 tests) — negative-control verified: re-introducing index alignment inside `DaySeries.slots` fails 5 of them; restored, 14/14 green on Sim F8ACD7F7 | implemented | row PR-108 |
| NFR-VIZ-DAY-02 (design-QA 2026-08-13) | **One "this week", app-wide — two screens may never disagree about the same signal.** Day-axis integrity (DAY-01) places values on the right days; this row pins the step before it: the SERIES itself must have one source. The sweep caught the Recovery pillar and the Insights week drawing Friday as the week's HRV **high** while the HRV clinical detail annotated "20 ms — your low, Friday" and the plain tier said "Yours dipped on Friday" — same window, same day letters, opposite claims. Root cause: `HRVLearnDetail` derives its week from the raw daily SDNN stream, while every other HRV surface plots `TodaySignals.hrvWeek`; with real data those are identical by construction, but `AppState.applyLV001DatasetIfNeeded()` REPLACES `todaySignals` with composed aggregates after derivation and left `hrvLearn` on the old stream. Rule: any surface reading a week-shaped HRV fact (series, extreme day, latest value, and the copy built from them) resolves it through the app's canonical week; the up-to-60-day statistics (range, median, own-usual band) stay with the deriver and name their own window in copy | `Liviqa/Intelligence/HRVLearnDeriver.swift` (`HRVLearnDetail.anchored(toWeek:)`, `HRVLearnDeriver.canonicalWeek(_:now:)`, `reconciled(_:with:now:)` — pure Foundation, NFR-PORT-01), `Liviqa/Views/LearnView.swift` (topic entry point reconciles + hands the same `DaySlot` axis to the chart), `Liviqa/Views/ChatView.swift` (assistant summary reads the reconciled detail), `Liviqa/Views/MetricDetailView.swift` (Recovery pillar's window now comes from `DaySeries.days(endingOn:count:)` — one window rule) | `LiviqaTests/HRVLearnDeriverTests` +4: `namedExtremeMatchesTheCanonicalWeek` (4 week shapes, each with the raw stream deliberately disagreeing — asserts series, named extreme day, low value and latest all come off the canonical week), `reconciliationActuallyMovesTheNamedDay` (negative control: the un-reconciled detail names a DIFFERENT day, so the fixture can't go stale), `datedCanonicalWeekPlacesEachValueOnItsOwnDay` (gapped week), `noCanonicalWeekLeavesTheDerivedWeekAlone`; build + build-for-testing green (`build/ddq2`); arithmetic cross-checked standalone against `DaySeries` | implemented | row PR-110 |
| NFR-UI-WEIGHT-01 (design-QA 2026-08-13) | **Hero weight follows the verdict, not the domain.** RK-ALARM-01 reserves clinical red for the glucose TIR charts; this row closes the gap it left — a non-red token can still borrow alarm WEIGHT by filling the largest surface on the screen at full saturation. The Heart hero carried the reassuring "Resting lower than your usual band." as a full-bleed crimson plate in paper / bright rose in midnight, the two themes reading at visibly different emotional weight. Rule: the full-bleed plate is reserved for the verdict that actually asks the reader to look; every calm or descriptive verdict gets the quiet plate (card ground + tint wash + tint edge + tint kicker + ink type), and the weight is chosen on the SAME branch that picks the sentence so the two cannot drift. TREATMENT ONLY — `accentHeart` 0xD9486B/0xF07E9B is untouched (CN-approved 2026-08-12), and because the quiet plate is alpha over each theme's own card ground it reads at equal weight in paper and midnight | `Liviqa/Views/MetricCharts.swift` (`MetricHeroWeight`, `MetricHero.weight` — `.solid` stays the default, so Sleep/Glucose/Vitals/Body/Fitness/Activity/Learn are unchanged), `Liviqa/Views/HeartDetailView.swift` (`Model.needsAttention` set on the verdict branch) | `LiviqaTests/MetricDetailCopyTests` — `calmVerdictsGetTheQuietHero`, `onlyTheWorthALookVerdictEarnsTheSolidHero`, `weightAndVerdictNeverDisagree` (6 RHR values); before-evidence `a72_designqa_20260813/pillar_heart_paper.png` + `pillar_heart_midnight.png`; re-shoot owed on the next sweep — the Simulator is owned by another session tonight | implemented | row PR-110 |
| NFR-UI-LABEL-01 (design-QA 2026-08-13) | **A chart's own labels may not collide, clip, or run to the card edge.** Four instances closed: the glucose day-curve peak annotation printed inside the plot's top-right corner — the corner the clinical zone key uses — overprinting the "HIGH" band label in midnight and running flush to the card edge in paper; the Vitals verdict word was drawn at `w−26` while the dot strip was sized to `w−78`, so the last dot and "Typical" landed on top of each other (and the longer "Worth a look" ran off the card); the Sleep depth soundings are CENTRED on their stage, so a stage late in the night hung off the right edge ("REM 1h 25m"). Rule: reserve a real gutter for every fixed label, and clamp any position-derived label inside the plot — the same clamp the wake-up marker already used | `Liviqa/Views/GlucoseDetailView.swift` (peak moves to a padded caption row under the curve, full-sentence copy "Highest today 11.2 mmol/L at 13:40"), `Liviqa/Views/MetricCharts.swift` (`DotBandStrip` three-column geometry with a 72 pt trailing verdict gutter, trailing-flushed; `SleepDepthChart.clamped(_:lo:hi:width:)` + fixed sounding width) | build green (`build/ddq2`); before-evidence `a72_designqa_20260813/pillar_glucose_midnight.png`, `pillar_vitals_paper.png`, `pillar_sleep_paper.png`; geometry re-derived by hand for a 338 pt card (last dot 258 pt vs verdict from 296 pt; REM sounding clamped 317→301 pt); re-shoot owed on the next sweep — the Simulator is owned by another session tonight | implemented | row PR-110 |
| NFR-COPY-01 (design-QA 2026-08-13) | **Say a direction once, and never name a fixture in prose.** (a) The Activity hero read "−2 % fewer steps than your usual" — the sign and the words both said "less", so the sentence claims the opposite of the data. The number now carries the size and the words the direction, in both directions and in the design seed. (b) T-DED-06's rule (a demo seed is never named in prose — the clause is dropped, the disclosure stays on the `isDemoData` chip) now also covers the metric-detail source slots: the Sleep hero was printing "… Core 5h 31m · Sample data", a demo BADGE standing where a device name belongs | `Liviqa/Views/ActivityDetailView.swift` (`Model.init(derived:)` + `designSeed`), `Liviqa/Views/MetricCharts.swift` (`MetricSourceLabel.inProse`/`isFixture`), `Liviqa/Views/SleepDetailView.swift`, `Liviqa/Views/GlucoseDetailView.swift` (hero sub + TIR foot — the foot no longer falls back to claiming "Apple Health" over a fixture) | `LiviqaTests/MetricDetailCopyTests` (below/above/level/no-history + seed parity) and `LiviqaTests/MetricSourceLabelTests` (real devices survive; "Mock"/"Sample data"/"Demo" dropped, case- and whitespace-insensitive; "Mockingbird Ring" still a device; the Sleep sub line end to end); build-for-testing green (`build/ddq2`) | implemented | row PR-110 |
| NFR-UI-EDGE-01 | Scroll-edge treatment on every scrolling reading surface, at BOTH edges: content must not run raw under the status bar / Dynamic Island, and must not be hard-cut by the floating tab bar. Two fades in the page's own `paper` token (marine in the evening edition) — the top one covers the window's real top safe-area strip plus a 22 pt bleed, the bottom one a 28 pt late-weighted feather; both firmer + shorter under Reduce Transparency. The top band's height comes from the WINDOW inset (`LiviqaWindowInsets.top`), never from a `GeometryReader` proxy: an `.overlay` is laid out inside its host's safe area, and `.ignoresSafeArea` CONSUMES the inset it ignores, so the proxy reads 0 — that is exactly why the PR-108 attempt rendered a 14 pt, invisible band. Overlay only — never a mask or clip, so scroll content size, offsets and the DEBUG `LIVIQA_SCROLL_TO=bottom` snapshot hook are untouched; `allowsHitTesting(false)` so nothing becomes untappable. Complements `liviqaScrollEdgeSoft()`, which is a no-op below iOS 26 / with the Liquid Glass flag off | `Liviqa/Views/ScrollEdgeFade.swift` (`liviqaScrollEdge()`, `LiviqaWindowInsets`), applied in `TodayView`, `WeekInContextView`, `TrendsView` | build green (Sim, `build/ddq1`) + test build green; **device-verified on Sim F8ACD7F7** — before `a72_designqa_20260813/home_bottom_paper.png` (card header printed through the status bar) vs after `FINAL_home_bottom_paper/_midnight.png`, `FINAL_insights_bottom_paper/_midnight.png`: the Island now sits over an unbroken paper/marine field in BOTH themes, over cards and charts, and the bottom edge feathers instead of cutting. Scroll offset unchanged between before/after frames (same first visible card). Reduce Transparency branch is code-reviewed only — the Simulator will not accept the setting headlessly | implemented | row PR-108 → **fixed PR-111** |
| NFR-UI-EDGE-02 (design-QA 2026-08-13) | **Horizontal strips carry their own trailing content inset** — a day strip / chip rail must never end flush with (or sliced by) the screen edge or its container's padding. Journal's day strip auto-scrolls to a trailing spacer instead of pinning today's pill to the viewport edge; the Care plan-a-consult day rail bleeds to full width and carries the 20 pt page gutter as content padding, so the last chip scrolls clear instead of being cut by the container inset | `Liviqa/Views/JournalView.swift` (`calendarStrip`, `stripTrailingID`), `Liviqa/Views/PlanConsultView.swift` (day rail) | build green (iOS Sim, `build/ddq3`); before-evidence `a72_designqa_20260813/journal_midnight.png` (selected "T 13" flush/cut) + `care_plan_paper.png` ("Thu 20" cut); re-shoot owed on the next sweep — the Simulator is owned by another session tonight | implemented | row PR-110 |
| FR-JRNL-PERSIST-01 | Private journal entries survive relaunch (MVP). Device-local JSON in Application Support with NSFileProtectionComplete (encrypted at rest, never uploaded); load on open (else demo seed on fresh install), auto-save on every add/edit/delete. **AMENDED 2026-08-13 by FR-JRNL-SCOPE-01/SEED-01 (row below): the file is per-ACCOUNT, not per-device; "else demo seed" is now DEBUG-and-demo-session only and can no longer be persisted** | `Liviqa/Security/JournalStore.swift`, `JournalView` (load + `onChange` save), `JournalModels` (Equatable) | build ✓; `JournalStoreTests` 4/4 (Codable + on-disk round-trip, missing-file nil); end-to-end verified on Sim | implemented (amended) | PR-58 → (this branch) |
| FR-ING-HINT-01 | Real-loop UX: on a real device with HealthKit tried but no readings, Home shows a calm, dismissible "Showing sample data → Connect Apple Health in Settings" hint that opens Settings; never in demo/screenshot mode | `AppState.showConnectHealthHint`, `TodayView.connectHealthHint`, `MainTabView` (wire + DEBUG `LIVIQA_FORCE_HINT`) | build ✓; forced-hint screenshot verified (/tmp/hint_s.png) | implemented | PR-58 |
| NFR-A11Y-01 | Dynamic Type scales body content; fixed chrome (tab bar, Home chips) clamped so it never wraps at accessibility sizes; chart accessibilityValue summary; VoiceOver labels on icon-only controls; care thread loading state | **`Theme.swift` font helpers (UIFontMetrics / `relativeTo:` — the actual scaling)**, `MainTabView`, `TodayView`, `OuraComponents` (AreaTrendChart), `EvidenceComponents` (NavBackHeader), `MessagesView` | build ✓; accessibility-extra-large genuinely scales — re-verified `/tmp/dt_default.png` vs `/tmp/dt_xxxl.png` | implemented (**genuinely** as of PR-97; PR-58's mark was on a false premise — `Font.custom(_:size:)` does NOT auto-scale, FB-AEkAWxal) | PR-58 → **fixed PR-97** |
| NFR-A11Y-03 (design-QA 2026-08-13) | **The chrome survives accessibility text sizes.** (a) No tab-bar label collides with, or truncates against, its neighbour — a destination name shrinks (to 70 %) and may wrap to two lines, but is never cut. The bar's own type growth is capped at ≈xxLarge by measuring the live `UIFontMetrics` scale and dividing it back out: `.dynamicTypeSize(...)` alone CANNOT clamp Liviqa fonts, because every one of them resolves through `UIFontMetrics`, which reads the system content-size category and never sees the SwiftUI environment — the pre-existing cap on the bar was therefore inert. (b) A chart y-axis column sizes to its widest real label instead of a fixed width, so "100%" is never clipped to "1…" and "92%" never wraps. (c) Scrolling screens reserve the bar's MEASURED height (+20 pt), not a hard-coded 96, so the floating bar never overlaps content at any text size | `MainTabView` (`barTypeScale`, `TabBarHeightKey`, `contentBottomInset`, `tabRow`/`tabCell`), `OuraComponents` (`AreaTrendChart.yAxis`, `GlucoseCurveView` y-axis) | build green (Sim, `build/ddq1`) + test build green; **device-verified at `content_size accessibility-large` on Sim F8ACD7F7** — before `a72_designqa_20260813/home_a11ylarge.png` / `insights_a11ylarge.png` / `pillar_glucose_a11ylarge.png` vs after `FINAL_a11y_home.png` / `FINAL_a11y_insights.png` / `FINAL_a11y_glucose.png`: all six labels render in full with visible gutters, the Insights TIR axis reads 100 % / 92 % / 86 % on one line each, and the tab bar clears the Glucose TIR key card instead of slicing it. Content size reset to `medium` after capture | implemented | row PR-111 |
| NFR-UI-V2-06 | Rich detail screens — tapping a Home wellness pillar opens an Oura-depth DESCRIPTIVE detail: Sleep stages timeline (Deep/Light/REM/Awake, mins+%), week trend for the others, big value + delta + descriptive observation + "Discuss in the assistant". No scores/verdicts/advice (non-MDSW) | `MetricDetailView`, `TodayView` (pillar NavigationLinks) | build (iOS Sim) ✓; sleep-stages detail screenshot verified | implemented | PR-56 |
| FR-VIZ-01 | Clinical Time-in-Range zones on the glucose chart — international-consensus AGP bands (very-low/low/target/high/very-high; target 3.9–10.0), descriptive only; red used ONLY here (RK-ALARM-01), never as a general app alarm | `OuraComponents.GlucoseCurveView` (`showsClinicalZones`), `MetricDetailView` (`clinicalTIRZones`, default on), `Theme.swift` (`tirVeryLow…tirVeryHigh`) | build ✓; real component rendered on the iOS 26 Simulator | implemented | PR-100 |
| FR-VIZ-02 | Graded deviation heatmap — the 5 personal-deviation levels shown as a calm cool→warm magnitude ramp capped at deep amber (never red) + non-colour outlier ring; colour-blind-safe | `WeekInContextView` (`fill`/`readout`/legend, `gradedHeatmap` flag), `Theme.swift` (`devMed/devHigh/devOutlier`) | build ✓; ramp validated in the Glass Lab | implemented (CN signed off; default ON, flag for instant revert) | PR-100 |
| FR-VIZ-03 | Cross-source correlation cards on Insights (lab-vs-lived glucose, money↔sleep, dining↔recovery) — the multi-domain "Apple can't" differentiator; observation-not-causation grammar + per-card sample size + "a pattern, not a diagnosis"; runs on SIMULATED pitch data (D5) so it ships **default OFF** (real users never see fabricated data) behind an opt-in Settings toggle | `CorrelationCards.swift` (`CorrelationCard`, `HbA1cDriftChart`, `FinanceSleepChart`, `AlcoholHRChart`, `CrossSourcePatterns`), `WeekInContextView` (`crossSourceCards` flag), `SettingsView` toggle, `Theme.swift` (`accentSleep/accentFinance/tirHigh`) | build ✓; validated in the Glass Lab | implemented (flag default OFF; opt-in for pitch/demo) | PR-100 |
| FR-WATCH-01 | Liviqa Watch app — glanceable, DESCRIPTIVE wellness companion on the wrist (calm state line + Glucose/Sleep/Recovery/Heart pillars, two-state); **tap-through pillar detail** (value + two-state observation + last-7-day sparkline); WatchConnectivity descriptive snapshot from phone; WidgetKit face complication (in-range fact). No scores/verdicts/advice (non-MDSW). watchOS/Widget targets created in Xcode | `LiviqaWatch/*` (WatchHomeView, WatchPillarDetailView+WatchSparkline, WatchModels, WatchTheme, LiviqaWatchApp, Connectivity/WatchSessionReceiver, Complication/*); **iOS sender wired**: `Liviqa/Connectivity/PhoneWatchSync.swift` (relocated into the iOS target's synced folder) + `AppState.syncWatchGlance()` (called from `refreshFromHealth`, mirrors `TodayView.signalRow` values/clay + week `trend` series; glucose stays time-in-range %, no provenance) | iOS build ✓ (sender compiles, no-op until paired); watch source builds once target exists | iOS push wired; watchOS/Widget targets pending Xcode (`LiviqaWatch/README_SETUP.md`) | PR-54/55 + local |
| NFR-UI-OPTS-03 | Runtime Display options (Theme Midnight/Paper, Reduce-motion, Demo-data-chip) — all real, `@AppStorage`-backed, consumed by live code; demo-chip default OFF (no `demo` label on customer UI per cardinal rule) | `Liviqa/Views/SettingsView.swift` (`displaySection`), `Liviqa/Views/MainTabView.swift`, `OuraComponents.swift` (`LiviqaMotion`) | build (iOS Sim); Paper switch verified end-to-end | implemented | PR-41 |
| FR-ARCH-05 | Real-vs-cohort integrity marker available but not rendered by default (dignity/credibility) | `liviqaShowDemoChip` (`SettingsView`, `MainTabView`), `TodayView` `isDemoData` | build (iOS Sim); chip absent by default verified | implemented | PR-41 |

| FR-ING-01 (full capture) | Full HealthKit read set: MVP types **+ insulin, AFib burden, BP, body-comp, heart/respiratory panel**; new value types + readers + extended arbitration + idempotent persistence into existing `@Model` entities; still read-only (empty write set) | `Liviqa/Ingestion/HealthSamples.swift`, `HealthKitService.swift`, `SourceArbiter.swift`, `IngestionCoordinator.swift` | T-HK-RO-02 (`readSetIsFullCaptureSet`), T-MAP-01/02 | **verified** (iOS Sim; 78/78) | PR-46 |
| FR-ING-01/02 (onboarding) | HealthKit primer "Connect" requests read authorization + switches provider to `.healthKit` (was a no-op leaving the app on demo); skip → demo, never error (A7.2: wiring moved into the unified flow; skip surfaces the declined screen first) | `Liviqa/Views/Onboarding/OnboardingFlowView.swift` (step 3 onConnect), `Liviqa/Views/HealthKitPrimerView.swift`, `AppState.dataProviderKind`/`refreshFromHealth` | (UI; screenshot) | verified | PR-46 |
| FR-ING-09 (idempotency) | Window-replace deletes the **union** of the requested window and the inserted rows' span → re-sync converges (no duplicate accumulation); no data dropped | `Liviqa/Ingestion/IngestionCoordinator.swift` (`bounds`, replace helpers) | T-MAP-02 (`reSyncIsIdempotent`) | **verified** (was failing pre-PR-46) | PR-46 |
| FR-REG-04 / OD-11 (data layer) | Insulin read **dose-blind** (data source only, no surface); AFib read **display-only** — the new HealthKit reads add no rendering/interpretation | `HealthKitService.readInsulin`/`readAFib`, `Entities.swift` (`InsulinDose`/`AFibBurden`) | RISK PR-46/47; T-NDG-02/03 | verified (data-layer only) | PR-46 |
| NFR-UI-CLAY-01 / FR-REG-02 | Patient attention tone = **clay** (not amber): no alarm/threshold framing; two-state moss=in-range/clay=notice; colour never the sole signal (label+position+metadata) | `Liviqa/Theme.swift` (`clay*`, `conf*`, type scale, `Tracking`), `Liviqa/Views/*.swift` (amber→clay sweep) | build (iOS Sim, both modes); Today screenshot moss→clay | verified | PR-47 |

_Rows below added 2026-07-03 (PR-102): the launch audit found these 8 FR-/NFR- ids live in code with no RTM row (cardinal violation — FR-QMS-03). Each names its covering test or an explicit test gap._

| FR-RSCH-03 | Join a research study = a fresh, scoped, aggregate-only consent grant to the sponsor + `consentGranted` ledger event; invitation consumed on action; nothing shared before approval | `Liviqa/Models/ResearchModels.swift` (`AppState.joinStudy`), `Liviqa/Views/StudyConsentView.swift` | test gap: `T-RSCH-01_joinStudy_createsScopedGrantAndLedgerEvent` (PR-99 changelog claims `T-RSCH-01` authored — no such test exists in `LiviqaTests/`) | implemented (untested — consent-critical) | PR-99 → row PR-102 |
| FR-WAL-08 | Reactivate withdrawn consents legally: revocation stays one-way in the ledger; each reactivation = a NEW explicit grant (fresh id + timestamp) + `consentGranted` event, never a silent un-revoke | `Liviqa/Sharing/ConsentReactivation.swift` (`AppState.reactivateWithdrawnConsents`), `Liviqa/Views/WalletView.swift` (entry when withdrawn grants exist) | test gap: `T-CONSENT-REACT-01_reactivate_createsFreshGrant_keepsRevokedEvent` (PR-99 changelog claims `T-CONSENT-REACT-01` authored — no such test exists in `LiviqaTests/`) | implemented (untested — consent-critical) | PR-99 → row PR-102 |
| FR-PROV-02 | Dual-recording disclosure: one physical session recorded by two trackers is counted ONCE (overlap+type cluster, tier wins primary, enrich from duplicate) and disclosed to the citizen — never silently fixed. **COPY RULE (design-QA 2026-08-13):** the disclosure sentence is the one place a source name is interpolated into prose, so a demo seed is NEVER named there — `DataSourcesView.mergeSentence` drops the clause ("Merged Bike computer · Strava") instead of substituting the demo label, and the "Sample data" label moves to a chip on the card header. Renaming the fixture to "Sample data" in the sentence (PR-109) still read as a fixture name standing in a device slot | `Liviqa/Ingestion/WorkoutDeduplicator.swift`, `SourceArbiter.swift` (cluster keying), `Liviqa/Views/DataSourcesView.swift` (`deviceName`/`gainedField`/`mergeSentence` + disclosure card), `AppState.dualRecordingMerges` | T-DED-01..05 (`WorkoutDedupTests`); **T-DED-06 = `SourceCopyHonestyTests`** (5 cases: named devices survive; demo seed never in prose on either side; enrichment fields keep the field and drop the seed; case-insensitive fixture match) | implemented (tests authored; run in Xcode — this branch built `build-for-testing` only, the Simulator is owned by another session tonight) | row PR-102 → copy rule row PR-110 |
| FR-PAT-01 | Long-horizon pattern detectors: computed wording, threshold gating, same contracts as the console engine; surfaced as pattern cards in demo builds only | `Liviqa/Intelligence/PatternEngine.swift`, `PatternSeed.swift` (`Nudge(finding:)`), `AppState.refreshFromHealth` (`if !real` gate) | T-PAT-01..04 (`PatternEngineTests`) | implemented (tests authored; run in Xcode) | row PR-102 |
| FR-PAT-02 | Compute `PatternInput` from the user's REAL device history (summarisation pipeline). Until it lands, pattern findings run only on the LV001 demo seed, in DEBUG, on the mock provider, on an UNMARKED day — three gates, all in `refreshFromHealth` | `Liviqa/Intelligence/PatternSeed.swift` (the only `PatternInput` producer in the app — verified 2026-08-13: nothing derives one from `HealthSamples`), `AppState.refreshFromHealth` (`demoPatternsAllowed` is `#if DEBUG` + `provider.kind == .mock`; the append is `if !real, !markedToday`) | pipeline still ungated by a test — named gap `T-PAT-05_patternInput_derivedFromDeviceHistory` (author with the pipeline). What IS covered today: `PatternEngineTests` (detectors + empty input ⇒ no findings) and **`HealthReadOutcomeTests.markedDaySuppressesDemoPatternFindings`** (FR-CTX-04: these cards are appended AFTER the engine, so they bypassed the suppression gate — a marked day now removes them, and the feed can only ever shrink) | planned — **row corrected 2026-08-13**: the requirement itself (real-history `PatternInput`) is genuinely unbuilt, but the previous row understated the guard (it named only `if !real`) and named no passing test. The context-flag hole it hid is fixed | row PR-102 → (this branch) |
| FR-PAS-03 | Glucose TIR band calibrates to the user's care-plan range (parameterised `tirLowMmol`/`tirHighMmol`; default = consensus 3.9–10.0 mmol/L) | `Liviqa/Intelligence/PassportStatsDeriver.swift` (`derive(tirLowMmol:tirHighMmol:)`), `TodaySignalsDeriver.swift` (pass-through) | `tirRespectsCustomBand` (`PassportStatsDeriverTests`) | in-progress (deriver parameterised + tested; `AppState` call sites still pass the default band — `ClinicalTargets` wiring open) | row PR-102 |
| FR-ING-08 | Meal-time metadata retained on glucose through the pipeline (`mealContext`, e.g. "pre-meal"/"fasting"; optional, validated entity field) | `Liviqa/Ingestion/HealthSamples.swift` (`GlucoseReading.mealContext`), `Entities.swift` (`GlucoseSample.mealContext`), `IngestionCoordinator.swift` (mapping) | test gap: `T-ING-08_mealContext_persistsThroughMapping` | in-progress (schema + mapping carry it; `HealthKitService` maps `nil` — HK meal-time metadata read open; mock sets "fasting") | row PR-102 |
| NFR-RSCH-04 | Research anonymity-set size k ≥ 5 (aggregate-only cohorts); k disclosed to the citizen in the consent copy | `Liviqa/Models/ResearchModels.swift` (`ResearchStudy.cohortK`), `Liviqa/Views/StudyConsentView.swift` (k ≥ 5 disclosure) | test gap: `T-RSCH-04_studyWithKBelow5_isRejected` | in-progress (declared on the model + disclosed in UI; no code gate rejects k < 5 — enforcement open) | row PR-102 |
| FR-GDPR-01 [PROV] | Export my data (GDPR Art. 20): Settings export button fetches the REAL server blob (`GET /me/export`) on the sovereign backend (honest device-local JSON on mock/demo) and hands the file to the share sheet — the former fake "Export ready in Files" animation is gone | `Liviqa/Views/SettingsView.swift` (export button + `ActivityShareSheet`), `AppState.exportMyData()`, `LiviqaBackendService.exportMyData()` (`DataRights`) | `T-GDPR-01` (`EraseOrderingTests.exportWritesServerBlobToShareableFile`) | implemented (tests green in iOS Simulator) | PR-103 |
| FR-GDPR-02 [PROV] | Erase everything, SERVER FIRST (GDPR Art. 17): `POST /me/erase` must confirm BEFORE the local wipe runs, so a network failure can never strand server data behind a success message; failure = retryable honest error state, nothing removed anywhere | `AppState.eraseEverythingServerFirst()`, `LiviqaBackendService.eraseMyData()`, `Liviqa/Views/SettingsView.swift` (delete flow states 1–4 + copy) | `T-GDPR-02/03` (`EraseOrderingTests`: failure aborts pre-wipe; success ordering server→local) | implemented (tests green in iOS Simulator) | PR-103 |
| FR-JRNL-SYNC-01 | Real journal sync — the `LiviqaBackendService` stub trio (fetch→`[]`, upsert echo, delete no-op) is replaced with the citizen `/journal` routes (`GET`/`PUT /journal`, `DELETE /journal/{id}`); server rows carry TEXT + timestamp only, mood/tags/metric snapshots stay device-local by design | `LiviqaBackendService` (journal trio + `journalBackendIDs`), `BackendMapping.journalEntry(from:)`, `JournalModels.swift` (full init) | `T-JRNL-SYNC-01..04` (`JournalSyncMappingTests`) | implemented (mapping tested; live loop pending route parity on `api.liviqa.app`) | PR-103 |
| FR-WAL-CE-01 [PROV] | Consent receipts in the ledger (P7 evidence, display wave): `WalletGrant.ceGrantRef` + `WalletEvent.ce` (`CEEvidence`: receipt_id/grant_ref/contract_sig/event_hash/tx_hash/scope keys — optional, backward-compatible decode); ConsentLedgerView shows a per-event "Evidence receipt" (copyable receipt id, short hash, "Verified by the DATA for GOOD consent ledger") and claims "verified" ONLY when evidence exists. On-device cryptographic verification of the receipt signature = flagged follow-up | `Liviqa/Models/WalletModels.swift` (`CEEvidence`), `LiviqaBackendService` (`LedgerEventDTO.Detail.ce`, `walletEvent(from:)`), `Liviqa/Views/ConsentLedgerView.swift` (`evidenceReceipt`) | `T-CE-01..05` (`ReceiptDecodeTests`) | implemented (decode + display; no on-device crypto this wave) | PR-103 |
| FR-COLD-01 [PROV] | Honest cold start (Release): NO mock seeding anywhere — seeds are empty via `ColdStart` (DEBUG keeps demo); first real run backfills 90 days of HealthKit history so baselines fill from existing data; genuinely empty ⇒ honest "Building your baseline / first insights after ~3 days" state, "—" signal chips, no seeded sparklines | `Liviqa/ColdStartSeeds.swift`, `AppState` (seeds, `initialBackfillDone`, `isColdStartEmpty`, gated loadWallet/research/demo seeding), `Liviqa/Views/TodayView.swift` (`baselineBuildingCard`), `JournalView`/`InAppPrivacyView` seed gates | `ReleasePostureTests` (gate lint) + Release build | implemented | PR-103 |
| NFR-REL-01 | RELEASE CONFIG IS TESTPROD: rails unified (`walletRailBaseURL` sandbox detour retired; DEBUG-only `LIVIQA_WALLET_RAIL_URL` override), runtime posture preconditions at launch, and a source-lint test suite so demo-gate regressions fail the build/test run, not a review | `Liviqa/Config.swift` (`walletRailBaseURL`), `Liviqa/ReleasePosture.swift` (`verify()` from `LiviqaApp.init`), `LiviqaTests/ReleasePostureTests.swift` | `ReleasePostureTests` (11 checks) + Release `xcodebuild` green | implemented (deploy gating = owner: route parity on `api.liviqa.app` first) | PR-103 |

| FR-ING-14 [PROV — SRS v06 pending] | In-app Sundhed.dk connect, SELF-ACCESS only: Path A live MitID WKWebView session (interceptor captures the SPA's own responses; credentials never visible to Liviqa) + Path B PDF import. Terminal sink = on-device store ONLY (auto-upload removed). NOTE: supersedes FR-ING-11's "tap inert pending RQ-05" posture — connect is live in Release citing a sanctioned self-access test; the written sanction artifact + DPIA v02 are OPEN owner gates (2026-08-12 audit blockers B1/B2) | `Liviqa/Views/SundhedWebSessionView.swift`, `Liviqa/Sundhed*`, `Config.sundhedWebConnectEnabled` / `sundhedConnectEnabled`, `Liviqa/Views/TodayView.swift` (connect + record cards) | **T-SUND-01 = `LiviqaTests/SundhedSinkTests.swift`** (8 tests, GREEN 2026-08-13): the on-device sink is TERMINAL. Both capture functions (`SundhedImportView.save`, `SundhedWebSessionView.ingest`) write to `AppState.ingestHealthRecord` exactly ONCE and contain no transport/grant construct (`ingestSundhed` · `ensureCoveringGrant` · `createGrant` · `pushDerivedShare` · `URLSession`/`URLRequest`/`httpMethod`/`httpBody`/`dataTask`) · no file under `Liviqa/Sundhed/` calls the ingest client, and the ONLY call site app-wide is `AppState.contributeHealthResearch` (FR-RSCH-05) · `ensureCoveringGrant` has ZERO call sites — the automatic-upload path has not returned · the live path carries no `/ingest/sundhed` in code · the shared `ingestHealthRecord` sink terminates in `store.ingest` with no egress · the canonical rows are registered in the on-device store with `cloudKitDatabase: .none`. Source-lint by necessity (Path A is a live MitID WKWebView, Path B a file picker; the regression is the ADDITION of a call). Mutation-checked 2026-08-13: reintroducing `await ensureCoveringGrant()` into the save path fails 2 tests. STILL OPEN: extraction fixtures (honest none-found states manually verified 10.94–10.98) | implemented — RELEASE-ENABLED; legal gates open | PR-104 |
| FR-REC-01 [PROV — SRS v06 pending] | Canonical on-device health record store: labs/conditions/meds from ANY source; per-source dedup + supersession; file-protected SwiftData, cloudKit `.none`; leaves the device ONLY via FR-RSCH-05. Save failures surfaced, never swallowed (Phase 0 `530b32d`) | `Liviqa/Health/HealthRecordStore.swift`, `Liviqa/Models/Domain/Persistence.swift` (schema + protection), `AppState` (`storeOpen`/`storeDegraded`, `ingestHealthRecord`) | **T-REC-01 = `LiviqaTests/HealthRecordStoreTests.swift`** (8 tests, GREEN 2026-08-13 — behavioural, real in-memory `ModelContainer`): re-ingesting the same reading from one source stays ONE row · the same observation from TWO sources keeps both rows (each tagged) but is COUNTED ONCE in `latestObservations()` · conditions dedup per (code, source) and collapse to one presence entry across sources in the research payload · medications dedup by ATC, else case-insensitively by name · a newer value for the same day+source SUPERSEDES in place (value replaced, `importedAt` refreshed, no second row) · an earlier DAY is kept as history with the newest leading and `priorObservation` returning the citizen's own previous value (nil when there is none) · `ingest()` is authoritative about the source stamp · the real `canonicalize` mapper round-trips a derived summary into coded rows (catalog scopeKey, upper-cased ICD-10, ATC, `mpcScaled`). Mutation-checked: dropping `$0.source == src` from the observation dedup predicate fails the two-source test. Store-open fallback still covered by construction + Home banner | implemented | PR-104 |
| FR-REC-02 [PROV — SRS v06 pending] | Diagnoses display: plain-language names + start year + major/minor tiering — DISPLAY-ONLY presentation of the citizen's own national record. No interpretation, no coupling into the nudge engine (FR-NDG-06 guard untouched; T-NDG-06* green 2026-08-12) | `Liviqa/Views/TodayView.swift` (record cards), diagnoses views (10.94–10.98), `HealthDisplay` naming | **T-REC-02 = `LiviqaTests/HealthRecordStoreTests.swift`** (4 tests, GREEN 2026-08-13): `conditionTier` groups by ICD-10 chapter without clinical judgement (15 codes incl. the Danish SKS `D`-prefix normalisation `DE104`→E104, chronic-vs-acute J-codes) · tiering NEVER hides an entry — the store returns every filed code whatever its tier, the shareable summary lists all of them under a named secondary heading, and always carries the explainer · every string the diagnoses surface can emit (the explainer, `conditionContext` for all noted codes and both tier-generic branches, `conditionName` across all 22 chapter letters) passes the FR-NDG-06 designated control `NudgeGuard.check` — no diagnostic claim, no normality verdict, no treatment directive · source-lint: nothing under `Liviqa/Intelligence/` references `HealthCondition`/`HealthObservation`/`HealthStore`/`conditionTier`/`icd10`, so the nudge engine stays structurally blind to the record | implemented | PR-104 |
| FR-RSCH-05 [PROV — SRS v06 pending] | Research contribution of the canonical record is EXPLICIT-ONLY: a single user-initiated path (`contributeHealthResearch`) builds the coded catalog v0.4.0 body; never auto-called; failure surfaces backend copy | `AppState.contributeHealthResearch()`, backend `/ingest/sundhed` (flag-gated, JWT-bound) | **T-RSCH-05 = `LiviqaTests/ResearchContributionTests.swift`** (8 tests, GREEN 2026-08-13). NO-AUTO-CALL (source-lint — the regression is the ADDITION of a call): `contributeHealthResearch()` has exactly ONE call site in the whole app, its enclosing block is a `Button` action, and no enclosing scope is a lifecycle/refresh path (`.task` · `.onAppear` · `.onChange` · `.refreshable` · `.onReceive` · `.onOpenURL` · `postSignIn` · `restoreSession` · `refreshFromHealth` · `loadWallet` · `init`); AppState reaches the ingest seam from exactly one place. PAYLOAD (behavioural, real `AppState` + fake `SundhedIngesting`): an EMPTY record transmits nothing at all · a backend failure never sets `researchContributed` and surfaces the backend's own copy · the transmitted body is the coded catalog v0.4.0 shape (catalog vars in `by_variable`, ATC `coded_dist`, ICD-10 `coded_dist`) and carries no brand name, no condition label, no display-only analyte and no unit string from one — a display-only analyte with no catalog code stays visible to the CITIZEN but never leaves the device · the body is an AGGREGATE (one entry per variable, exactly one timestamp — the `as_of` stamp). Mutation-checked: adding `contributeHealthResearch()` to an `.onAppear` fails 2 tests | implemented | PR-104 |
| NFR-REL-02 | No silent persistence failure states: ingest save surfaced to the user; degraded-store (in-memory fallback) banner on Home; Release binary cannot be steered onto the mock provider (forcing branches compiled out) | `HealthRecordStore.ingest` (throws), `AppState.storeDegraded`, `TodayView.storeDegradedBanner`, `AppState.resolveProviderKind` (`#if DEBUG`) | `ReleasePostureTests.providerForcingIsDebugGated` + full-suite run 2026-08-12 | implemented (tests green) | PR-104 |

_Rows below added 2026-08-12 (row PR-107, A7.2 full-screen program): the 74-screen census (`Liviqa_A72_ScreenCensus_v01_20260812.md`, DfG Works Liviqa root — 23 MISSING / 31 ANATOMY-DIFFERS / 1 RESKIN-ONLY / 19 MATCHES) drove the SRS v06 diff proposal (`Liviqa_SRS_v06_DiffProposal_A72_20260812.md`). Every id below is `[PROV — SRS v06 pending]` until CN ratifies. Id-space note: the census's "UC-20 (proposed)" is re-keyed to UC-25 in the proposal (UC-20 is taken in UseCases v04); on ratification the earlier NFR-REL-01/NFR-REL-02 rows above are re-keyed to NFR-REL-04/NFR-REL-03 (their labels collide with different SRS v05 meanings)._

| FR-TOD-05 [PROV — SRS v06 pending] | Evening edition + transparent decomposed day score: per-segment visible arithmetic vs the user's own data (sleep/8h, TIR, HRV vs own week mean), never an opaque composite (anti-score-opacity); 30-day month trend must come from a real deriver before rendering for real users | `Liviqa/Views/TodayView.swift` (`isEveningEdition` branch: closingNote/scoreCard/monthTrendCard/tomorrowHook), `Liviqa/Intelligence/TrendsDeriver.swift` (30-day HRV series — the named deriver extension, landed A7.2 Area ②; `TodayView.recoveryTrend` consumes it) | named gap T-TOD-05 (score arithmetic; 30-day series covered by `TrendsDeriverTests.monthRangeCarriesHRVDailySeries` + `DayAxisAlignmentTests.hrvSlotsCoverTheWholeWindowSoTheMonthCardLabelsAreTrue`) — **RAIL: FR-NDG-06** closing verdicts allow-listed (T-NDG-06* blocking, never skipped) | in-progress (anatomy + decomposed score live PR-106; REAL 30-day month trend live via TrendsDeriver; auto-palette decision open). **2026-08-13**: the month trend is placed by DATE (`TrendsRange.hrvSlots` → `MonthTrendLine.slots`) over all 30 columns, so its edge date labels are true and days without a reading break the line instead of being drawn through; DEBUG `LIVIQA_EDITION` now also drives the greeting so forced-edition QA frames are internally consistent (Release keeps the real clock) | row PR-107 |
| FR-TOD-06 [PROV — SRS v06 pending] | Trends surface: wired from Today (momentum "See the trend →" pushes TrendsView; DEBUG `LIVIQA_OPEN_TRENDS=1`); daily TIR vs the user's own usual band (mean ± 1σ of own daily TIR) per Week/Month/Quarter; correlation cards ONLY past the conservative evidence gate (\|r\| ≥ 0.4, two-tailed p ≤ 0.05 via critical-r table, N ≥ 10 paired days; STRONG at \|r\| ≥ 0.6) with N·r·p always on; aggregate tiles delta'd vs the previous equal window; honest empty/still-learning states — the old canned demo copy is deleted and cannot render on any account | `Liviqa/Views/TrendsView.swift` (v03 rebuild), `Liviqa/Intelligence/TrendsDeriver.swift` (new), `Liviqa/Views/TrendsCharts.swift` (new — TIRTrendBarChart), `Liviqa/Views/TodayView.swift` (momentum link + env hook), `AppState.trends` | T-TOD-06 = `TrendsDeriverTests` (gate refusal at small N, guard-clean templates, band/aggregate derivation, honest nil) + `DayAxisAlignmentTests` (day axis: every TIR/HRV value keeps its own date; gapped windows leave gaps, never a shifted bar or a zero) — **RAILS held: red=TIR-only** (Trends chart is moss/fjord personal-band, no red) + **FR-NDG-06** (no "STRONG" below the gate; copy guard-checked) + OD-07 mmol/L | implemented (A7.2 Area ②; tests run on Sim F8ACD7F7). **2026-08-13 — day-axis correctness**: the TIR chart is drawn from `TrendsRange.tirSlots` (one column per CALENDAR day from the first charted day to the window end) instead of a bare `[Double]`; axis labels derive from the slot dates; the hero's "lately…" tail is dropped when the last charted day is more than 2 days old | row PR-107 |
| FR-XPL-01 [PROV — SRS v06 pending] | Universal "See why" + published method notes (Bevel absorb ②): EVERY verdict surface expands to its baseline-relative decomposition — which numbers, over what window, against which personal baseline, and the arithmetic where a figure is composed; the weighting is published in plain language in the Learn tier. A surface that cannot decompose itself from real data must say so (`unexplained`) rather than print a blurb. The two places Liviqa leans on a SHARED reference (70% TIR mark; the 8-hour sleep leg of the day score) are NAMED on the surface instead of passing as "your usual" | Decomposition source: `Liviqa/Intelligence/SeeWhyExplainer.swift` (new — pure Foundation; no new derivation, formats what the derivers already produced; owns `todayTone` + `dayScoreLegs` so the printed verdict and the printed arithmetic are chosen by ONE function). Presentation: `Liviqa/Views/SeeWhySheet.swift` (new — one `SeeWhyChip` affordance + one `SeeWhySheetView` disclosure + `.seeWhySheet` modifier). Wired on: `TodayView` (day hero · evening closing note · marked-day register · all four signal cards · the evening day-score card), `NudgeDetailView` (asserted + still-learning), `MetricDetailView` (recovery), `SleepDetailView`, `GlucoseDetailView`, `HeartDetailView`, `FitnessDetailView`, `ActivityDetailView`, `BodyDetailView`, `VitalsDetailView`, `MetricBaselineView`. `TrendsView` correlation cards keep their always-on N·r·p chips (A7.2 Area ②). Learn tier: `Liviqa/Views/LearnView.swift` — HRV article (A7.2 Area ⑨, figures from `Liviqa/Intelligence/HRVLearnDeriver.swift` with REAL window-day labels) **plus four published METHOD NOTES** (`usual-band`, `day-score`, `evidence-gate`, `verdict-words`), each deep-linked from the matching disclosure; method notes carry no live figures by construction. Area ⑨ entry points now reachable: `LearnArticleView(topic:.hrv)` from `MetricDetailView` (recovery), `MetricBaselineView` (HRV row) and the Today recovery card's disclosure; `@AppStorage("literacyLevel")` still selects the default tier with the deeper layer one tap away | **T-XPL-01 = `LiviqaTests/SeeWhyExplainerTests.swift` + `LearnMethodNoteTests`** (same file): every fixed template across every builder and branch through `NudgeGuard` (>40 explanations: surface, verdict echo, row labels + values, honest-absence copy, footer) · all four method notes through `NudgeGuard` · honest-absence proven (sample seeds and cold start emit `unexplained` and NO rows; a <4-day series refuses to split the week; an unlearned band is stated, not approximated) · day-score legs match the published formula incl. caps and the honest denominator · `todayTone` drives the matching rows · window rows report REAL coverage · no population/normality phrases · the verbatim personal-baseline footer on every disclosure. `HRVLearnDeriverTests` still covers the HRV article — **RAIL: FR-NDG-06** (every new generated string through the allow-list; designated control, blocking) + personal-baseline-only + red=TIR-only (the disclosure is moss/trust, never an alarm colour) | implemented (Bevel absorb ②). Divergences: (a) the Home Heart card's verdict word was the CONSTANT "Calm" whatever the reading — an unexplainable verdict — and now reads off the same own-usual band the card already draws (falls back to "Calm" when no band is learned, i.e. unchanged for <4 days of readings); (b) `MetricDetailView`'s canned recovery observation ("higher meeting load and later meals") was rendering over REAL readings and is now gated to the illustrative values it was written for; (c) Home signal cards changed from `NavigationLink(value:)` to Button + `navigationDestination(item:)` so the card carries two real tap targets (open the pillar · see why) without nesting a control inside a link. Not shipped: a disclosure on calibrating skeleton cards (no verdict ⇒ nothing to explain); Danish pass is a separate gate | row PR-107 |
| FR-CTX-04 [PROV — SRS v06 pending] | Context status flag (travelling/unwell/off-routine, Bevel absorb ③): DECLARED by the user (never inferred); suppresses baseline-comparison nudges on a marked day; honest "Calibrating around your trip" register on Today + calm stand-in for the attention slot (ONE-attention-card rule held); week-grid columns render NEUTRAL (flat slate + shared `ZoneHatch`, excluded from cluster/hard-day counts, `.noData` still empty) — no fake data, no deviation alarms, no red. Marked days stay in every derivation and every aggregate: the flag changes how a day is READ, never what it contains | `Liviqa/Models/ContextFlag.swift` (model, note is display-only), `Liviqa/Intelligence/ContextFlagDeriver.swift` (`ContextWindow` + suppression gate — kind+dates only, no note field), `Liviqa/Security/ContextFlagStore.swift` (device-local JSON, `NSFileProtectionComplete`, wiped by `deleteAllData`), `Liviqa/Intelligence/NudgeEngine.swift` (`context:` gate), `AppState` (`contextFlags`/`activeContextFlag`/`contextWindows`, `markContext`/`endContextFlag`/`removeContextFlag`), `Liviqa/Views/ContextFlagSheet.swift` (entry + review), `TodayView` (register + entry row + quiet note), `WeekInContextView` (neutral columns, legend, readout, verdict), `TrendsView` (marked-days annotation), `SettingsView` ("Days you've marked") | **T-CTX-04 = `LiviqaTests/ContextFlagTests.swift`** (13 tests: control-fires / suppression / ended-flag / flag-alone-generates-nothing / **marked-output-is-a-subset-of-unmarked** / AFib-route-survives / inclusive+open ranges / day-granular / markedCount / latest-start-wins / guard-clean copy / store round-trip + erase / window-drops-the-note) — executed via `swiftc` harness, ALL GREEN 2026-08-13 — **RAIL: FR-NDG-06** interaction is suppression-only (T-CTX-04d/e prove it structurally: a flag can never create a nudge nor raise a priority) + safety route never suppressed (T-CTX-04f) + red=TIR-only (marked tint is `accentFinance` slate); FR-ARCH-05 honest states; `/qms/RISK.md` RK-CTX-01 section added | implemented (Bevel absorb ③). **Per-BAR neutral rendering on the Trends TIR chart NOW SHIPPED (2026-08-13)**: `TrendsRange.tirDailyDates` + `tirSlots` give every bar its own calendar day, `TrendsView.markedDates` passes the marked days by date, and `TIRTrendBarChart.neutralDates` draws them in the week grid's flat `accentFinance` + `ZoneHatch` treatment — never the deviation ramp, height still exactly as recorded. The marked-days annotation strip and the held-back "lately…" tail both stay | row PR-107 |
| FR-FIT-01 [PROV — SRS v06 pending] | Fitness & exercise detail: training load, per-workout avg HR, Z1–Z4 time-in-zone, VO₂max long trend from persisted history; no targets ever ("Only your own week-to-week change matters — there is no target" designated) | `Liviqa/Views/FitnessDetailView.swift` + `Liviqa/Intelligence/FitnessDeriver.swift` (load = duration × intensity, published arithmetic; zones from OWN observed max HR); **ingestion (T-FIT-01, closed 2026-08-13): `HealthKitService.readWorkoutHeartRate` reads `heartRate` bounded by each workout's interval (one OR-compound predicate, strict start+end, last `hrWindowDays = 28` only) into `HealthSamples.workoutHeartRate`; `SourceArbiter.arbitrated()` carries the stream; `FitnessDeriver.derive` reads it by default**; entry `HealthPassportView` + `WellnessPillar.fitness` (LIVIQA_OPEN_PILLAR) | `FitnessDeriverTests` (load arithmetic, buckets/own-usual, HR-gated zones, template guard) + **`LiviqaTests/WorkoutHeartRateIngestionTests.swift`** (5 tests, GREEN 2026-08-13): ingested beats unlock avg-HR + zones with no extra wiring; arbitration does not drop the stream; a session with no beats keeps avg-HR and the zone card honestly ABSENT; the zone reference is the citizen's OWN observed maximum and the card's foot prints it ("shares of 160 bpm — the highest your own workouts recorded… not a target"); the demo provider carries beats that all fall inside a workout interval and stay SIMULATED — **RAIL: FR-NDG-06** held (foot + headline + verdict through `NudgeGuard`); **personal-baseline-only**: no age formula (220−age), no population zone scale, no target; no red on this surface | implemented (A7.2 Area ④ + T-FIT-01). Open: VO₂max trend still spans the live 30/90-day fetch window — the persisted-history extension is a separate item | row PR-107 → (this branch) |
| FR-SLEEP-02 (intra-night ingestion) [PROV — SRS v06 pending] | Sleep segments ingested WITH their wall-clock start and the AWAKE stage, and a night bucketed by the day it ENDS on — so the Sleep detail's depth chart ("the night as depth"), the wake-up moment, the AWAKE tile and bedtime consistency render from the citizen's own night instead of a demo seed. Bedtime is compared to the citizen's OWN mean bedtime only — never a recommended hour, no target | `Liviqa/Ingestion/HealthKitService.swift` (`readSleep` keeps `s.startDate`, drops only `.inBed`; `nightDay(_:)` = start-of-day of *t*+6 h so a 23:04→06:14 night is ONE bucket, not two half-nights split at midnight), `Liviqa/Ingestion/HealthSamples.swift` (`SleepReading.start` optional + `intervalStart/intervalEnd`; `mergedAsleepHours` unions on the real start), `Liviqa/Intelligence/SleepDetailDeriver.swift` (`SleepNightShape`, `SleepBedtimeWeek`, `awakeMin`, `nightShape`, `bedtime`), `Liviqa/Views/SleepDetailView.swift` (`depthCard`/`bedtimeCard`, demo seeds only when there is no derivation at all), `Liviqa/Ingestion/MockDataProvider.swift` (demo nights laid out in time, deterministically — stage totals unchanged) | **`LiviqaTests/SleepNightShapeTests.swift`** (7 tests, GREEN 2026-08-13): a timed night derives segments + soundings carrying the REAL stage totals + a wake-up with its real clock time; an untimed night derives `shape == nil`, `bedtime == nil`, `awakeMin == 0` and keeps the reduced anatomy (nothing is reconstructed); AWAKE never inflates an asleep total; the nightly union uses the real starts (regression: a fragmented night used to collapse to its longest fragment because every segment shared one instant); bedtime needs ≥3 timed nights and is measured against the citizen's own mean; the demo provider produces timed nights. Existing `SleepMergeTests` / `SleepDetailDeriverTests` / `SleepDeriverTests` unchanged and green — **RAILS: FR-NDG-06** (depth headline, wake-up sub-line and bedtime headline through `NudgeGuard`) + **personal-baseline-only** (the bedtime card's foot states "\"Usual\" here is the average of your own bedtimes this week. There is no recommended hour on this page.") | implemented (this branch) | (this branch) |
| FR-ING-02 (inferred-denial cue) [PROV — SRS v06 pending] | A read request that COMPLETES with zero samples across every requested type surfaces the honest "nothing came through" screen instead of dropping the citizen into a silently empty app. It must NOT claim access was denied: HealthKit does not report read authorisation (`authorizationStatus` answers for writing only), so a refused read and a brand-new watch are indistinguishable — the calmer wording is used and both explanations are offered | `Liviqa/AppState.swift` (`HealthReadOutcome` + pure `readOutcome(kind:samplesEmpty:)` + `healthReadReturnedNothing`), `Liviqa/Views/Onboarding/OnboardingFlowView.swift` (the connect path now AWAITS the first fetch and branches on its answer), `Liviqa/Views/HealthKitPrimerView.swift` (`isConnecting` state while the sheet + fetch are in flight), `Liviqa/Views/Onboarding/HealthAccessDeclinedView.swift` (`Reason.skipped` / `.noReadings` copy pair) | **`LiviqaTests/HealthReadOutcomeTests.swift`** (4 tests, GREEN 2026-08-13): only a real Health read can produce a verdict (a mock/demo session says nothing about Apple Health, ever); a demo session never surfaces the cue; the no-readings copy contains none of {denied, refused, blocked, "you did not allow"}, says outright that the two cases can't be told apart, and passes `NudgeGuard` — **RAIL: honest-claims** (the app never asserts something it cannot observe) | implemented (this branch) | (this branch) |
| FR-VIT-01 [PROV — SRS v06 pending] | Vitals dot-band strips (SpO₂ · resp rate · VO₂max): last-14-nights dots vs the user's own typical band (mean±σ), verdict word "Typical" — never clinical reference ranges | `Liviqa/Views/VitalsDetailView.swift` + `Liviqa/Intelligence/VitalsDeriver.swift` + `DotBandStrip` (`Liviqa/Views/MetricCharts.swift`); entry `HealthPassportView` + `WellnessPillar.vitals`; VO₂max's long trend deliberately lives on the Fitness screen (one canonical home, no duplicate surface) | `VitalsDeriverTests` (own-band ≥5-day gate, 14-night strip, verdict words limited to the fixed vocabulary, template guard) — **RAILS held: personal-baseline-only** (band = own mean±σ, foot copy says "not a clinical reference") + **FR-NDG-06** (fixed templates guard-tested) ; design-QA 2026-08-13: the strip's verdict word overlapped the last dot on both charts — geometry reworked to three columns with a real trailing gutter (NFR-UI-LABEL-01) | implemented (A7.2 Area ④) | row PR-107 / PR-110 |
| NFR-SEC-08 [PROV — SRS v06 pending] | Face ID / app lock: opt-in `LocalAuthentication` (`.deviceOwnerAuthentication`, passcode fallback), scene-wake lock overlay, Account toggle + onboarding offer; "No passcode leaves the device" stays literally true (no custom PIN storage) | `Liviqa/Views/Onboarding/AppLockSetupView.swift` (setup offer + `AppLockScreen` runtime, LAContext `.deviceOwnerAuthentication`), scene-phase overlay in `LiviqaApp.swift` (`appLockEnabled`/`isLocked`), `SettingsView` My-data toggle, `AccountSecurityView` toggle (Area ⑧ — same `@AppStorage` pref, not a rebuild), `Info.plist` `NSFaceIDUsageDescription` | **T-SEC-07 = `LiviqaTests/AppLockTests.swift`** (12 tests, GREEN 2026-08-13 — source-lint: `LAContext` cannot be evaluated in a unit test and the wiring lives in a `Scene` body). ENGAGES: a cold launch starts locked by reading the pref at `@State` init (no flash of content); the scenePhase observer locks on `.background` while enabled and can NEVER unlock; the lock is an `.overlay` on the ROOT group; the only `isLocked = false` in Release is `AppLockScreen`'s unlock callback (the other is DEBUG-gated). SYSTEM AUTH: every `canEvaluatePolicy`/`evaluatePolicy` in the app uses `.deviceOwnerAuthentication` and `deviceOwnerAuthenticationWithBiometrics` appears NOWHERE (it has no passcode fallback and would lock a citizen out); no persistence API is ever combined with a passcode/PIN concept and the lock screen has no `SecureField`/`TextField` — so "No passcode leaves the device" is literally true. FAILS OPEN: the `guard canEvaluatePolicy … else` block in `AppLockScreen.attempt()` calls `onUnlock()` and does not set the failed state, while a genuinely FAILED attempt keeps the lock and offers a retry; enabling is gated on `canEvaluatePolicy` on all three surfaces (onboarding · Settings · Account) and DISABLING is never gated; `NSFaceIDUsageDescription` is declared. Mutation-checked: swapping in the biometrics-only policy fails the suite. RAIL: designated NFR-SEC claim accuracy | implemented (A7.2 Area ①; Account-screen surface Area ⑧; T-SEC-07 tests written and green 2026-08-13) | row PR-107 |
| FR-SET-02 (persisted posture) [PROV — SRS v06 pending] | Backup posture PERSISTED across launches and surfaces; "Sovereign EU cloud" visibly "Coming soon" and non-selectable (RQ-06); no copy implies a backup exists before an engine does | `Liviqa/Views/Onboarding/OnboardingSteps.swift` → `BackupStep` (writes `@AppStorage("backupPreference")` on Continue, restores it in `onAppear`, mirrors it to `AppState.backupPreference`; the `.sovereign` card is built with `selectable: false` + amber "Coming soon" chip and its button body `guard`s on that), `Liviqa/Views/AccountSecurityView.swift` (reads the SAME `backupPreference` key), `BackupPreference` (`Liviqa/Models/LifestyleModels.swift`) | **T-SET-02b = `LiviqaTests/BackupPostureTests.swift`** (3 tests, GREEN 2026-08-13): the chosen posture round-trips through the one shared key for all three cases; an unset or legacy/corrupted value falls back to `.onDevice` — never to a posture that implies a backup exists; `AppState.backupPreference` starts `.onDevice`. The non-selectable `.sovereign` card is a view-level posture, verified by inspection (no UI test on this step) — RAIL: honest-claims (no implied backup); RQ-06 gate | implemented — **row corrected 2026-08-13**: the row still claimed the selection was `@State`, "discarded on complete". It has not been since the Area ① onboarding work: it is `@AppStorage`, written on Continue and read back by Settings. Status, evidence and test name all updated | row PR-107 → (this branch) |
| FR-NOT-01 [PROV — SRS v06 pending] | Notification settings v1, edition-model ladder ("Calm by default"): morning edition / earned attention / evening wind-down / care messages / study & consent activity (default off), persisted prefs wired to real scheduling where it exists (morning/evening local loops), honest status line for the channels that haven't launched. DECISION (Area ⑧): quiet-hours removed with the old metric-category model — the edition ladder is time-shaped by design (fixed morning note / post-21:00 close / max one earned alert) | `Liviqa/Views/NotificationSettingsView.swift` (v02 rebuild, `@AppStorage` via `EditionNotifications.Pref`), `Liviqa/Services/EditionNotifications.swift` (new — prefs, allow-listed templates, `resync()`), `Liviqa/Services/PushNotifications.swift` (study-pref gates the foreground banner of study payloads) | T-NOT-01 = `NotificationPrefTests` (designed defaults incl. study-OFF, persistence round-trip, template guard-check, pref-shaped request builder) — **RAIL: FR-NDG-06 cadence** — "At most one a day" restated verbatim; notification content is static allow-listed template text ONLY (no derived text path exists in `EditionNotifications` by construction) | implemented (A7.2 Area ⑧; tests authored, run in Xcode) | row PR-107 |
| FR-NOT-02 [PROV — SRS v06 pending] | Calm notification micro-loops (Bevel absorb ⑤): morning ping / single earned-attention alert / evening close as on-device local notifications, deep-linking to their edition surfaces; text-only | Clock loops: `Liviqa/Services/EditionNotifications.swift` (morning 07:30 + evening 21:30 `UNCalendarNotificationTrigger` daily locals, silent, fixed template text, resynced at launch `LiviqaApp` + on every pref change). **Earned-attention loop now REAL (2026-08-13)**: `Liviqa/Services/BackgroundRefresh.swift` (`BGAppRefreshTask`, identifier `$(PRODUCT_BUNDLE_IDENTIFIER).refresh`, registered in `AppDelegate.didFinishLaunching`, `Info.plist` `BGTaskSchedulerPermittedIdentifiers` + `UIBackgroundModes:fetch`, re-armed on background + pref change), `Liviqa/Services/EarnedAttention.swift` (pure picker — allow-listed category gate, engine priority ≥ 60, 09:00–20:00 window, one-per-day ledger, fixed per-category templates, single replacing identifier, `trigger == nil` so the alert is derived and delivered in the SAME wake), `Liviqa/Services/NudgeRun.swift` (the ONE seam to `NudgeEngine` — foreground `AppState.refreshFromHealth` and the background wake both call it, so the off-session loop cannot drift from the screen), `PushNotifications.swift` (tap → `.liviqaOpenEdition` / `.liviqaOpenAttention`), `AppState` (headline → card resolution, re-tried after the feed rebuilds on a cold launch) + `MainTabView` (Home for an edition note; the nudge's own evidence view for an earned alert), `NotificationSettingsView` (status line states the iOS-decides limit verbatim) | T-NOT-02 = `EarnedAttentionTests` (FR-NDG-06 guard over every deliverable string **plus** a no-digit rail so no reading can reach a banner; delivered text ≠ engine sentence; one-per-day cadence + ledger round-trip + single identifier; eligibility — AFib route, verdict, number and sub-60 priority all stay silent; delivery window + next-wake aiming; request shape + deep-link tag re-checked against the guard). T-NOT-01 (`NotificationPrefTests`) still covers the clock loops — **RAIL: FR-NDG-06 designated** — delivered content is the static per-category allow-list; the engine's own sentence never travels; content never leaves the device | implemented **with a stated limit**: iOS grants the wake opportunistically, so the loop can go quiet. Every failure mode is SILENCE — never woken / woken while locked (HealthKit **and** the file-protected FR-CTX-04 context store both unreadable ⇒ refuse rather than decide without the suppression gate) / woken outside the window / nothing earned. No branch can deliver a stale or invented alert. Device QA of a real BG wake outstanding | row PR-107 |
| FR-WID-01 [PROV — SRS v06 pending] | Home-screen widgets + richer complications (Bevel absorb ①): edition verdict SENTENCE + decomposed signal chips + compact two-state TIR, morning/evening edition variants; **no composite score ring** (the structural answer to Bevel's score-opacity complaint); honest timestamped staleness ("as of HH:MM") because a widget always renders cached state | **iOS widget (source-complete, target-pending):** `LiviqaWidgets/Widget/LiviqaWidgetBundle.swift` (@main bundle) + `EditionWidget.swift` (entry/`TimelineProvider`/`Widget`, `.systemSmall` + `.systemMedium`) + `EditionWidgetViews.swift` (layouts, `TwoStateRangeBar`, `SignalChipView`, `StalenessLine`; LiviqaTheme tokens only). **Data plumbing:** `LiviqaWidgets/Shared/LiviqaWidgetSnapshot.swift` — tiny Codable snapshot + App-Group store (`LiviqaWidgetSnapshotStore`, suite id read from Info.plist `LiviqaAppGroup` = `$(APP_GROUP)`, NEVER hard-coded, fails CLOSED when absent); `LiviqaWidgets/Shared/WidgetCopy.swift` (single table of every static user-facing string, so FR-NDG-06 can sweep it); app-side writer `LiviqaWidgets/App/WidgetSnapshotPublisher.swift` (`WidgetSnapshotComposer` + `AppState.publishWidgetSnapshot()`, mirrors `syncWatchGlance()`). **Watch complication:** `LiviqaWatch/Complication/LiviqaComplication.swift` — all four families finished (circular / rectangular / corner / inline; the dedicated `.accessoryInline` branch retained), snapshot-backed with legacy `watch.inRange` fallback, `containerBackground` + `widgetAccentable` + per-family accessibility, no hard-coded suite string. **Setup:** `LiviqaWidgets/SETUP.md` (numbered Xcode click-path for both targets). | T-WID-01 (snapshot wire format carries no `provenance`; key set is an allow-list) · T-WID-02 (FR-NDG-06 sweep of all static widget copy + violating-verdict fallback) · T-WID-03 (composer decomposes, never fabricates; edition rule) · T-WID-04 (honest "as of" staleness across day boundaries) · T-WID-05 (TIR two-state + clamped) · T-WID-06 (store fails closed without an App Group) — all in `LiviqaWidgets/Tests/LiviqaWidgetSnapshotTests.swift`. **RAILS: FR-NDG-06** allow-listed output only, re-checked by `NudgeGuard.check` in the publisher before any sentence is written (violation ⇒ neutral line, never the offending text) + **provenance-never-renders** (T-PROV-01 extended: `bash scripts/guard_provenance.sh LiviqaWidgets` green 2026-08-13) + red=TIR-only (widget TIR is moss/`line` two-state; `clinRed` and the `tir*` ramp appear nowhere in `LiviqaWidgets/`) | **in-progress — source-complete, TARGET-PENDING (2026-08-13).** Both extension targets still do not exist; `project.pbxproj` remains unedited by hand (repo rule upheld). VERIFIED THIS PASS: (a) a new top-level `LiviqaWidgets/` folder is NOT auto-compiled — only `Liviqa` / `LiviqaTests` / `LiviqaUITests` are `PBXFileSystemSynchronizedRootGroup`s and the app target lists only the `Liviqa` group; proved with a `#error` canary that did not fire while the app build stayed green; (b) iOS widget sources `swiftc -typecheck` clean vs iPhoneSimulator26.5 (arm64-ios17.0-simulator) together with `Liviqa/Theme.swift`; (c) complication + Shared typecheck clean vs WatchSimulator26.5 (arm64-watchos10.0); (d) app-side publisher compiles against the real `AppState` (temporarily staged into the app target, then removed); (e) app build green, `guard_provenance.sh` green on `Liviqa`, `LiviqaWidgets`, `LiviqaWatch`. T-WID-01…06 are **written but NOT yet executing** — no target owns them, and `build-for-testing` is separately blocked by a PRE-EXISTING failure at `Liviqa/Health/LabReportOCR.swift:146` (`VNRecognizeTextRequest.requiresOnDeviceRecognition` missing under the Xcode 26.5 SDK; reproduced on a clean tree with none of these files present). The rail assertions behind T-WID-01/02/04/05/06 were instead executed in a standalone macOS harness against the real sources — all passed. Do not mark verified until the targets exist and the suite runs in Xcode. **BLOCKER for the human:** the App Group must be provisioned — `Config/Liviqa.ppcn.entitlements` deliberately omits App Groups for the PPCN beta, so on that signing path the store fails closed and the widgets show their honest empty state (see SETUP.md §1). | row PR-107 |
| FR-JRN-04 [PROV — SRS v06 pending] | Voice-note journaling: on-device recording + on-device transcription (`SFSpeechRecognizer`, `requiresOnDeviceRecognition = true` — hard-set in the ONE request factory); audio only in the local protected store (`journal-audio/`, `NSFileProtectionComplete`, wiped by GDPR erase); transcript into `JournalEntry.body` + `audioFilename` pointer (local-only field — journal sync DTOs never carry it); honest degradation to an audio-only note when on-device recognition is unavailable (no fake transcript, no server path exists in the capture code) | `Liviqa/Services/VoiceNoteCapture.swift` (new — `VoiceNoteCapture` + `VoiceAudioPipeline` + `VoiceNoteAudioStore`), `Liviqa/Views/VoiceNoteView.swift` (v02 — designed full-screen recorder, live transcript, real mic-level waveform), `JournalView` (fullScreenCover + entry wiring), `JournalModels` (`audioFilename`), `Info.plist` (mic string updated to cover voice notes, `NSSpeechRecognitionUsageDescription` added) | T-JRN-04 = `VoiceCaptureTests` (**designated**: `makeRequest().requiresOnDeviceRecognition == true`; store location; audio-pointer persistence round-trip; legacy decode) — **RAIL: on-device-only claim gating** — the "Transcribed on this iPhone — the audio never leaves it." footer renders ONLY while `onDeviceTranscription` is live; otherwise honest audio-only copy | implemented (A7.2 Area ⑧; tests authored, run in Xcode; DEBUG hook `LIVIQA_TAB=journal LIVIQA_OPEN_VOICE=1`) | row PR-107 |
| FR-ING-15 [PROV — SRS v06 pending] | Encrypted on-device document store realising FR-ING-12/13 ("Health data space"): AES-256 with Secure-Enclave-wrapped key, real add/import/persist/delete + metadata rows (name, kind, added date, source, byte size); distinct from the coded-observation HealthRecordStore | `Liviqa/Security/HealthVaultStore.swift` (AES-256-GCM over the KeyVault DEK, ECIES-wrapped to the SE P-256 device key; encrypted metadata index — no plaintext name on disk; `NSFileProtectionComplete`; `LocalUserScope` namespacing; `loadIndex` separates "no index" from "unreadable index" and `add`/`delete` REFUSE on the latter so a key that cannot read the index can never overwrite it; `VaultAccess` + `HealthVaultSession` name the four states a screen may be in), `Liviqa/Security/KeyVault.swift` (key TYPE = Secure Enclave else software P-256 → `protection`/`isHardwareBacked`; key STORAGE = Keychain else `DeviceKeyFileStore` — complete file protection, excluded from backup — used only when the Keychain answers errSecMissingEntitlement/errSecNotAvailable, as an unsigned simulator build does; existing-but-unusable key material raises `sealedKeyUnreadable` and is never re-keyed), `Liviqa/Security/CryptoCore.swift` (`KeyFailure.isDeviceLocked` / `.isStorageUnusable`), `Liviqa/Views/HealthVaultView.swift` rebuilt to ScrVault (add via Files/Photos, kind-icon list, preview sheet, delete-with-confirm; SE claim rendered from `KeyVault.protection` — enclave named only when enclave-held, no key claim at all before provisioning; recoverable states get a "Try again" card and never the unreadable sentence; "Add a document" enabled from `canAddDocuments`), `DataSourcesView` import wiring (picked files land in the store, not a toast), erase wipe in `AppState.eraseEverything` (2d); DEBUG hooks `LIVIQA_OPEN_VAULT` / `LIVIQA_OPEN_SOURCES` (`MainTabView`) | T-ING-15 = `HealthVaultStoreTests` (ciphertext-at-rest incl. index, delete-really-deletes on disk, metadata persistence across instances, GCM tamper → loud failure, scope isolation, kind inference, fresh space ready+writable, unprovisionable key recoverable-not-unreadable, sealed-data-unreadable reported AND non-destructive, add never overwrites an unreadable index) + `KeyVaultFallbackTests` (DEK stable across instances, hardware claim follows the live path, unusable key material never silently replaced, failure classification, device-file store round-trip/isolation/hashed names, `LocalUserScope` stability) — **RAILS held: AES-256 + Secure Enclave local-store rail** (VaultSeed demo retired — the "encrypted on this device" claims are now literally true, in every state) **; consent** — documents out of shares by default (finance docs never enter any clinician share; no upload path exists) — `/qms/RISK.md` RK-VAULT-01 + RK-VAULT-02 (Area ⑦ section) | implemented (A7.2 Area ⑦; robustness fix 2026-08-13 — the space now opens on unsigned/Keychain-less builds and separates recoverable key states from genuinely unreadable data; camera/scan capture remains a follow-up — Files + Photos import ship now) | row PR-107 |
| FR-DFG-07 [PROV — SRS v06 pending] | DfG token wallet surface (donate-first designed anatomy) over honest-data machinery: real ledger sums, honest Release empty states, no fabricated catalogue; tokens never carry health data | `Liviqa/Views/TokenWalletView.swift` + wallet-event logging for token events | T-DFG-07 partial: `ConsentSurfaceTests.donationDescriptionIsCauseNameOnly` (no health values in `TokenTransaction` descriptions) — **RAIL: consent-record claim gating HELD** — token events still don't log as wallet events, so the "written to your consent record" sentence is deliberately NOT shipped (footer claims only "tokens carry no health data"); extends FR-DFG-01/03 (no implied live entitlement) | implemented (A7.2 Area ⑥: brass→marine hero + donate-first anatomy + DEBUG-only cause catalogue with honest Release empty state; wallet-event logging for token events remains open — the claim ships with it) | row PR-107 |
| FR-RSCH-06 [PROV — SRS v06 pending] | Research participation hub: "Off by default · always your choice"; grouping promise in the A7.2 register ("grouped with at least 4 other people — never shown alone" = k≥5, a translation not a threshold change); open-studies directory with honest empty state (demo study never renders as real); join → StudyConsentView; token card; consent-record footer | `Liviqa/Views/ResearchHubView.swift` (new; entry `WalletView` Research row + DEBUG `LIVIQA_OPEN_RESEARCH=1`), `Liviqa/Models/ResearchModels.swift` (`ResearchStudy.groupingPhrase`) | T-RSCH-06 = `ConsentSurfaceTests.groupingPhrase*` (k≥5 floor clamp + exact canvas wording) — **RAILS held: consent** (off-by-default; hub renders only a genuinely surfaced `researchOpportunity`, honest empty state otherwise) + k≥5 (NFR-RSCH-04, `cohortK ≥ 5` stands) | implemented (A7.2 Area ⑥; open-studies FEED remains single-invitation — a real directory is backend work) | row PR-107 |
| FR-RSCH-07 [PROV — SRS v06 pending] | Study-consent data-category toggles default OFF (was: pre-selects all categories — consent-safety default fix); join disabled until ≥1 on | `Liviqa/Views/StudyConsentView.swift` (`initialSelection`/`canJoin` statics; joined state names the actually-selected scopes) | T-RSCH-07 = `ConsentSurfaceTests.studyConsentDefaultsAreAllOff` + `joinRequiresAtLeastOneCategory` — **RAIL: consent-safety default** — `/qms/RISK.md` RK-RSCH-07 row added (Area ⑥ section) | implemented (A7.2 Area ⑥) | row PR-107 |
| FR-WAL-09 [PROV — SRS v06 pending] | Share receipt slip (UC-21 surface): receipt-slip-first anatomy, QR/wallet offer demoted behind "Add to my DfG wallet"; proof number renders ONLY with real `CEEvidence`; while `CE_MODE=stub` receipts are NON-EVIDENTIARY — no "signed so nobody can change it" / "Nobody can edit this — not even us" overclaims (soften to "kept in your consent record") | `Liviqa/Views/ShareReceiptSheet.swift` (slip rebuild: `ReceiptSlipModel.build` + demoted QR retinted to A7.2 ink 0x2A4FAE), `Liviqa/Models/WalletModels.swift` (`CEEvidence.isEvidentiary`/`proofNumber`, `latestEvidence`), `Liviqa/Views/ConsentLedgerView.swift` (`headerClaim` gating; verified chip + evidence block evidentiary-only) | T-WAL-09 = `ConsentSurfaceTests` (evidence gating, slip proof-row/claim gating incl. stub-evidence case, header claim, evidence matching) — **RAILS: consent receipts; CE-stub non-evidentiary copy gating** — `/qms/RISK.md` RK-WAL-09 row added (Area ⑥ section) | implemented (A7.2 Area ⑥; citizen-credential offers keep QR-first; on-device receipt-signature verification stays the flagged follow-up) | row PR-107 |
| FR-PMS-01 [PROV — SRS v06 pending] | Report a wrong/harmful nudge (UC-19) — post-market surveillance intake: nudge id/tag/headline + shown-at + reason (5 fixed choices) + optional note as a SUMMARY of the pattern — the `PMSReport` type has no field that can carry a reading (summaries-only BY CONSTRUCTION); on-device file-protected outbox (queued offline; wiped by GDPR erase); consent-ledger-style receipt listing exactly what is queued; urgent-care footer. TRANSPORT: **queue-only** — the sovereign client (`LiviqaBackendService`) exposes no PMS endpoint today; the receipt says so honestly ("will send when a reporting channel opens"); the send loop lands with the backend route | `Liviqa/Views/ReportNudgeView.swift` (new sheet), `Liviqa/Security/PMSOutboxStore.swift` (new), `Liviqa/Views/NudgeDetailView.swift` ("Report this insight" affordance), `AppState.eraseEverything` (outbox wipe) | T-PMS-01 = `PMSOutboxTests` (payload key allow-list — summaries-only, round-trip, fixed reasons) — **RAILS held: FR-NDG-06** untouched (feedback loop only) + **consent/summaries-only** payload — `/qms/RISK.md` RK-PMS-01 row added (A7.2 Area ② section) | implemented (queue-only transport; tests authored, run in Xcode) | row PR-107 |
| FR-IDD-01 [PROV — SRS v06 pending] | Study-enrollment uniqueness check (UC-25; census working id "UC-20"): ID_METHODS-driven method picker, `dfg_mpc` (DfG Private Uniqueness Check) preferred + pre-selected; used ONLY to prevent duplicate trial entries — never as login; no raw biometric ever stored by Liviqa/DfG; declining keeps full personal use; every check writes a consent-ledger receipt; result never enters the health record | `Liviqa/Views/Onboarding/IdentityVerifyView.swift` (ID_METHODS local seed table, `dfg_mpc` pre-selected + PREFERRED chip, 4-step private-check explainer, 'Continue without verifying' always available); entry from `StudyConsentView` join flow (never auth); Partisia issuer/verifier round-trip + ledger receipt = future backend | named gap T-IDD-01 — **RAILS: consent ledger** (receipt per check); never-login; no-raw-biometric; Release posture (no fabricated verification result — none is fabricated: the check is not performed) | implemented UI-only (A7.2 Area ①; verification + receipt stubbed per allowed posture) | row PR-107 |
| FR-LIT-01 [PROV — SRS v06 pending] | Health-literacy preference (UC-26): plain/balanced/clinical stored on device (default balanced), consumed by depth-aware copy; deeper layer always one tap away ("Liviqa never hides your real data" designated); first consumers = metric-detail copy + chat plain-language template | `Liviqa/Views/Onboarding/OnboardingSteps.swift` (`LiteracyStep` A/B/C cards + same-night LevelFig previews, B pre-selected) + `@AppStorage("literacyLevel")` (plain|both|clinical, default "both"); FIRST CONSUMERS LIVE (A7.2 Area ⑨): `Liviqa/Chat/LiviqaChat.swift` (`LiteracyLevel` + plain-voice glucose templates — voice only, numbers always stated) + `Liviqa/Views/LearnView.swift` (`LearnArticleView` default-tier selection: clinical opens on the numbers, plain/both open plain, either tier one tap from the other); `MetricDetailView` copy still pending | T-LIT-01 partial: `ChatBehaviourTests.literacyShapesVoiceNeverHidesNumbers` (plain and clinical voices carry the SAME figures) — RAILS: OD-07 (clinical tier names mmol/L, GMI, HRV ms); never-hides-data promise must be genuinely true wherever depth-gating applies (held: Learn plain tier links "Show me the numbers"; chat plain voice keeps the % figure) | in-progress (capture + storage A7.2 Area ①; chat + Learn-tier consumers live A7.2 Area ⑨; metric-detail copy pending) | row PR-107 |
| FR-ARCH-06 [PROV — SRS v06 pending] | Honest Release sample-data mode (UC-02 amendment): clearly-marked sample-data-only mode for declined/skip paths and any future "Try it without an account"; inferred-denial cue (request completed + zero samples) surfaces the declined screen; demo sign-in stays DEBUG-only until this lands | declined screen: `Liviqa/Views/Onboarding/HealthAccessDeclinedView.swift` (ScrHealthDenied anatomy; surfaced by the primer's skip in `OnboardingFlowView`; Health-app deep link); `AppState` sample-data mode + inferred-denial cue still pending (`Liviqa/ColdStartSeeds.swift`, `HealthKitPrimerView` skip path) | named gap T-ARCH-06 — **RAILS: sample-data marking (T1)** + **provenance-never-renders** (the "sample data" chip is a neutral UI label, never the provenance field) | in-progress (declined screen implemented, A7.2 Area ①; inferred-denial cue + Release sample-data mode pending) | row PR-107 |
| FR-REC-03 [PROV — SRS v06 pending] | Any-lab PDF/photo import (Bevel absorb ④; built 2026-08-13 after CN sign-off): a PDF or photo from ANY lab is read on device — PDFKit text layer where the file has one, `VNRecognizeTextRequest` otherwise (fragments regrouped into rows so name+value+unit arrive together) — and a conservative recogniser maps an ALLOW-LIST of 15 analytes (HbA1c, glucose, total/LDL/HDL cholesterol, triglycerides, creatinine, eGFR, ALAT, TSH, haemoglobin, ferritin, CRP, vitamin D, B12) to canonical units. Anything else, any unit without an exact conversion, any ambiguous number ("1.234"), any censored bound ("<0.6") and any value outside the OCR-sanity bound is REFUSED and listed as an unread line with its reason — never guessed. Review-before-save mirrors Sundhed Path B: every row shown with its parsed value, its printed value when converted, the source line and the file name, editable and rejectable; low-confidence rows arrive switched OFF; only explicit approval writes into `HealthRecordStore` (`source: .paperScan`, `sourceDetail` = file name). Each row is framed against the citizen's OWN prior stored value for that analyte (same unit only) or stated plainly — never a population band or normality verdict. The document itself is DISCARDED after parsing unless the citizen opts in to the encrypted document vault. | `Liviqa/Health/LabReportParser.swift` (new, pure Foundation — allow-list, unit table, refusals), `Liviqa/Health/LabPriorFraming.swift` (new — the only generated text), `Liviqa/Health/LabReportOCR.swift` (new — PDFKit + Vision shim), `Liviqa/Views/LabReportImportView.swift` (new — review-before-save), `Liviqa/Health/HealthRecordStore.swift` (`priorObservation(scopeKey:before:)`, `.paperScan` realised, 4 display names), entries in `Liviqa/Views/DataSourcesView.swift` + `Liviqa/Views/HealthVaultView.swift`; the file option reuses `Liviqa/Security/HealthVaultStore.swift` unmodified | T-REC-03 = `LiviqaTests/LabReportParserTests.swift` (unit normalisation incl. mg/dL→mmol/L and IFCC→NGSP; allow-list closure; four refusal classes; reference bands never read; prior-value framing) — **RAILS held: OD-07** (glucose mmol/L, HbA1c = NGSP % headline via `SundhedParsers.hba1cIFCCtoNGSP`, no second standard) + **FR-NDG-06** (`everyFramingSentencePassesNudgeGuard` runs the designated control over every framing sentence, all 15 analytes, both directions) + **provenance-never-renders** (`scripts/guard_provenance.sh` green; `.paperScan`/`sourceDetail` are user-facing source labels, not the data field) + baseline-relative framing + no clinical red — `/qms/RISK.md` FR-REC-03 section (RK-REC-03a/b) | implemented (build green iOS Sim; parser logic executed and verified out-of-target — the XCTest run itself belongs to the session's device-QA pass) | row PR-107 |

| UC-01 / FR-AUTH-01..03 (A7.2 flow) | Unified designed onboarding (Area ①, 2026-08-12): cover → why → sign-in (real GoTrue states kept; demo entry DEBUG-only) → Apple Health primer (→ declined) → DfG governance (+ data-minimisation card, 2-dot sub-pager) → name capture (device-local `displayName`) → Health Passport tiles (reuse `ProfileSheet` editors) → sharing explainer → backup posture (sovereign gated 'Coming soon') → app lock offer → literacy → how-it-learns → ready. PrivacyDeclarationView stays FIRST (FR-REG-01); completion sets all legacy gates so version-replay logic is unchanged. Copy EN; provenance never rendered. **Design-QA 2026-08-13 (row PR-110):** off-path frames (`OnbFrame.isOffPath` — today only `.healthDeclined`) show NO "n/9" counter; the declined screen is where step 3 lands when you skip it, not step 3 itself, and "3/9" read as a false claim. The 9-segment bar keeps its position (orientation without the claim) and its VoiceOver label becomes "Setup progress — the Apple Health step" | `Liviqa/Views/Onboarding/*` (`OnboardingFlowView` frame chrome, `OnboardingSteps` `OnbPrimaryButton` → `primaryFill`, `FjordScene`, satellites), restyled `AuthView`/`HealthKitPrimerView`, `LiviqaApp.swift` gate, `AppState.setDisplayName` | DEBUG env hook `LIVIQA_ONB_STEP=<0..10|declined|lock|literacy|mitid|identity>`; per-frame Simulator screenshots (PR-110 evidence: `a72_designqa_20260813/onb_declined_paper.png`) | implemented | (this branch) → PR-110 |
| UC-consult (UI, A7.2) | Care & PRO citizen surface rebuilt to the A7 canvases (Area ⑤, 2026-08-12): Care tab = gradient live-consult hero (fixed fjord gradient + iris watermark + "Join secure consultation" pill) + inline thread rows with newest-message preview (`CareThread.lastMessagePreview`) + verbatim urgency footer (shared `CareUrgencyNote`, also above the thread composer); message thread gains org/role subtitle, `createdAt` date dividers, honest `readAt` read receipt; PlanConsult adopts the verdict header / "Request this consultation" / fallback-phone card (on-device copy kept); Pre-visit gains the "Enter waiting room" CTA (only when a consult is genuinely inside the −15…+30 min join window); waiting room = the LIGHT canvas treatment on dynamic tokens (Midnight = evening marine free of charge), 15-min grace state KEPT; incoming call reskinned (marine gradient + brass witness ring; consent line = canvas copy MERGED with the recording guarantee — strengthened, never weakened); ConsultView = full-bleed call stage (floating citizen-owned recording banner over the stage, summaries-only note pinned above controls, native leave; mic/cam/hang-up stay Jitsi-toolbar; FB 10.39 who-am-I-talking-to folded into the connecting shell). Code-ahead extras KEPT: research invite, proposal Accept/Decline, busy-slot strikethrough, amber/red device checks, grace state. | `MessagesView.swift` (+`CareUrgencyNote`), `PlanConsultView.swift`, `PreVisitCheckView.swift`, `WaitingRoomView.swift`, `IncomingCallView.swift`, `ConsultView.swift`, `CareConnect.swift` (`lastMessagePreview`), `MockData.swift` (DEBUG seeds) | DEBUG env hooks `LIVIQA_TAB=care LIVIQA_OPEN_CARE=<hero\|plan\|previsit\|waiting\|incoming\|consult>`; build (iOS Sim) — **RAILS: consent-first join (joining IS consent, structural), recording-consent copy unchanged/strengthened, no clinical red, provenance never renders, NFR-PRIV-05 (no trust chips on messages), FR-NDG-06 untouched** | implemented | (this branch) |
| FR-PRO-01 [PROV — SRS v06 pending] | Liviqa PRO enablers, iOS side (Area ⑤): the pre-visit "Share my consented metrics for this consult" tick, on the sovereign backend, ARMS a real per-consult grant — short-lived (24 h expiry), summaries-only granularity, purpose `consultation` — and pushes the derived summary (glucose TIR %/mean · sleep · recovery HRV, 7-day window) over the existing FR-SHARE-02 path (`DerivedShareBuilder`: no raw samples, no provenance, mmol/L per OD-07; GMI stays the HbA1c headline console-side). Un-ticking before the call REVOKES the exact grant the screen created (one-way; ledger keeps both events); failure reverts the tick — the UI never claims a share that didn't happen. Off the sovereign backend the tick stays local (nothing transmitted — honest demo posture). The PRO clinician screen itself is CONSOLE-side and unbuilt; iOS contract gaps for the console: `GET /threads` newest-message preview field; PRO view-events written into the citizen's consent ledger (must surface in `ConsentLedgerView` — Area ⑥ file, untouched). | `PreVisitCheckView.swift` (share gate + status line), `AppState.armConsultShare/disarmConsultShare` (+`consultShareGrantIds`), `SovereignSharing.revokeGrant` + `LiviqaBackendService` (`POST /grants/{id}/revoke`), `ThreadDTO.lastMessagePreview` | **T-PRO-01 = `LiviqaTests/ConsultShareTests.swift`** (9 tests, GREEN 2026-08-13 — behavioural: a fake `SovereignSharing` captures what AppState asks for, and a `URLProtocol` stub captures the real wire body). ARMING: creates exactly ONE grant with purpose `consultation`, scope groups exactly {glucose, sleep, recovery}, the recipient's own role, and an expiry 24 h out (`AppState.consultShareLifetime == 24 h`, asserted against the clock, not the constant alone); AppState requests NO explicit granularity, and the wire body the sovereign client actually POSTs to `/grants` carries `granularity` = `summary` for EVERY consented group. PAYLOAD: the pushed share lands under the grant just created, its metric keys are a subset of the derived set {tir, mean_g, hrv, rhr, sleep} (activity, never consented, is absent), every series point is a `yyyy-MM-dd`/`yyyy-MM` BUCKET and strictly fewer than the raw readings in the same 7-day window, and the encoded body contains no `provenance`/`simulated`/`"source"`. REVOKE: un-arming revokes exactly the id it created and forgets it, a second un-tick revokes nothing else, and a FAILED revoke keeps the id and surfaces the error so the citizen can retry. NEVER CLAIM: a failed `createGrant` returns false, remembers no grant, pushes no summary and sets `lastError`; off the sovereign backend nothing is created or transmitted; `PreVisitCheckView.toggleShare` reverts `shareConsent` on a failed arm and revokes on un-tick. Mutation-checked: widening the client's default granularity to `raw` fails the wire test. **RAILS: consent (summaries-only, expires with the grant), provenance-never-renders (payload carries none — now asserted, not just by construction), red=TIR-only is a CONSOLE obligation (no TIR renders on this iOS surface)** — safety-path change, `/qms/RISK.md` A7.2 Area ⑤ note | implemented (iOS enablers verified; console screen + view-event ingestion open) | (this branch) |
| UC-consult (stage honesty, 2026-08-13) | The in-call stage is never blank: the room webview reports its real load state (`WKNavigationDelegate` finished/failed/terminated) and any state short of loaded content is covered by `CallStagePlaceholder` — brass witness ring + who you're talking to + one derived line, plus a self tile that names the local camera fact ("No camera on this device" / "Camera off", read prompt-free from `AVCaptureDevice`). Copy comes from `CallStageDeriver`: `.live` prints NOTHING (the provider owns the surface), an unconfigured room may not say "connecting", an unreachable room says "Not connected" and offers a retry (re-issues the room request). Recording-consent banner + summaries-only note byte-identical. | `Liviqa/Intelligence/CallStageDeriver.swift` (new), `Liviqa/Views/CallStagePlaceholder.swift` (new), `Liviqa/Views/ConsultView.swift` (webview navigation delegate + reload token + camera probe; old `secureShell` folded into the placeholder) | `LiviqaTests/CareStageAndGranularityTests.swift` → `CallStageDeriverTests` (8 assertions: live draws nothing; every non-live phase has status+detail+self caption; no-room never says "connecting"; unreachable says so + retry; "waiting for your camera" only while connecting; device fact beats phase) + `LiviqaTests/CallStagePlaceholderRenderTests.swift` (renders the placeholder off-screen and counts lit pixels — the blank-rectangle failure mode itself is under test). GREEN 2026-08-13, sim F8ACD7F7. **RAILS: never imply a connection that does not exist; recording-consent + summaries-only copy unchanged; no clinical red; provenance never renders; FR-NDG-06 untouched (no generated text — fixed templates only)** — `/qms/RISK.md` RK-CONSULT-STAGE-01 | implemented | (this branch) |
| FR-SHARE-02 (consent granularity made explicit, 2026-08-13) | The consent surface states its detail level instead of relying on a backend default: `ShareGranularity` (pure Foundation) names the summaries-only map once, `ShareWithClinicianView.requestedGranularity` states it, and the screen's "— summaries only" suffix + new "Detail level" preview row are DERIVED from that map, so a wider level would strip the promise from the screen rather than leave a false one. **OPEN (AppState, one line): `AppState.swift:1056` `granularity: nil` → `granularity: ShareGranularity.summariesOnly(for: scopeGroups)`; then `ConsultShareTests.swift:135` flips from expecting `nil` to expecting the explicit map.** Wire behaviour today is unchanged (still summaries-only via `LiviqaBackendService.createGrant`'s `?? summary`). | `Liviqa/Intelligence/ShareGranularity.swift` (new), `Liviqa/Views/ShareWithClinicianView.swift` | `LiviqaTests/CareStageAndGranularityTests.swift` → `ShareGranularityTests` (every consented group carries a stated level; empty consents promise nothing; ONE wider entry breaks both `isSummariesOnly` and the printed copy; the copy is derived, not typed). Existing wire assertion `ConsultShareTests.theWireBodyCarriesSummariesOnlyGranularity` still GREEN. **RAILS: consent (summaries-only), derived-only share payload (FR-SHARE-02 / DerivedShareBuilder untouched)** — `/qms/RISK.md` RK-SHARE-04 | implemented (screen side); AppState one-liner OPEN | (this branch) |
| QA hooks (Area ⑤/⑥ screenshot family, 2026-08-13) | Two documented DEBUG hooks were inert because they only acted on a sheet that nothing opened, so a sweep captured the default screen. Now self-sufficient from Privacy: `LIVIQA_TAB=privacy LIVIQA_SHARE_PHASE=preview\|done` opens the share form AND jumps it to that phase (the form's own hook also moved AHEAD of the awaited recipient-directory fetch, which used to strand it for the whole network timeout on a backend build); `LIVIQA_TAB=privacy LIVIQA_RECEIPT_OFFER=1` now implies `LIVIQA_DEMO_RECEIPT=1`, so the receipt sheet opens and the demoted wallet offer is expanded. Both stay DEBUG-only env checks in the owning view's `.task` (existing idiom); no Release surface. | `Liviqa/Views/WalletView.swift` (DEBUG share-form sheet + receipt-offer implication), `Liviqa/Views/ShareWithClinicianView.swift` (hook ordering + recipient fill on both backends), `Liviqa/Views/ShareReceiptSheet.swift` (unchanged hook, now reachable) | Debug sim build green (`build/ddc2`); hooks compile-verified and reachable from `LIVIQA_TAB=privacy`. Frame capture belongs to the session's device QA (this agent does not launch the app). | implemented | (this branch) |
| FR-ACC-01 (A7.2 Area ⑧) | Account & security screen (b-integrations ScrAccount): verdict "Only your name and email live here." → signed-in row (HONEST: "Signed in" + real session email — no "with Apple" claim, the session model carries no provider) · Face ID app-lock toggle (NFR-SEC-08 pref, wrapped not rebuilt) · backup row stating only what is true (posture chosen in onboarding; NO backup engine exists — "nothing has been backed up yet") · recovery contact = honest "coming soon" note (no storage/endpoint exists) · real Sign out ("your on-device data stays" — signOut clears session state, files remain) → held-vs-on-device two-column card → delete entry → DfG wellness footer (single-sourced `RegulatoryCopy.dfgWellnessFooter`) | `Liviqa/Views/AccountSecurityView.swift` (new), `SettingsView` MY DATA nav row | build (iOS Sim); DEBUG hook `LIVIQA_TAB=settings LIVIQA_OPEN_ACCOUNT=1` — RAILS: honest claims only (three canvas claims downgraded, see file header), no red, provenance never renders | implemented | (this branch) |
| NFR-PRIV (delete, A7.2 Area ⑧) | "Delete all my data" moved from the inline Settings card to the designed dedicated screen (b-integrations ScrDeleteData): verdict / plain-words body / "What gets erased" list / "Or keep your documents" (REAL selective erase — `deleteAllData(keepDocuments:)` branches around exactly the Area ⑦ vault-clear step 2d; server erase identical since the server never holds documents) / shares-stop warning / danger+quiet CTAs. PRESERVED: server-FIRST ordering + retryable honest failure state (T-DEL-01/T1 — the canvas omits it). RETENTION WORDING: canvas "kept 30 days by law" NOT shipped — unverifiable against the backend contract; honest wording "kept only as long as the law requires" until the statutory period is confirmed (open compliance item, RISK Area ⑧) | `Liviqa/Views/DeleteDataView.swift` (new), `AppState.deleteAllData(keepDocuments:)` / `eraseEverythingServerFirst(keepDocuments:)`, entries in `SettingsView` (REGULATORY) + `AccountSecurityView` | T-DEL-01 unchanged (`EraseOrderingTests` still pass the default path); DEBUG hook `LIVIQA_OPEN_DELETE=1` | implemented | (this branch) |
| FR-REG-01 (single-sourcing, A7.2 Area ⑧) | MDR wellness-not-device notice single-sourced: the two DIVERGENT wordings (SettingsView regulatory card vs InAppPrivacyView) replaced by ONE canonical constant (the InAppPrivacyView long-form, per census); the Account-screen DfG footer lives in the same file so the sentences are reviewed together | `Liviqa/Models/RegulatoryCopy.swift` (new — `mdrNotice`, `dfgWellnessFooter`), rendered by `SettingsView` + `InAppPrivacyView` + `AccountSecurityView` | (review) — regulatory copy now changes in exactly one place | implemented | (this branch) |
| FR-JRNL-PERSIST-02 (A7.2 Area ⑧) | Declared health profile (About you / HealthContext) survives relaunch: device-local file-protected JSON store, loaded at AppState init, saved on ProfileSheet Save, wiped by GDPR erase (the census found edits lived only in memory). Plus the ScrProfileSheet delta: collapsed conditions render as display chips ("Type 2 diabetes · since 2019") with "+ Add a condition", tap-to-edit | `Liviqa/Services/HealthContextStore.swift` (new), `ProfileSheet.swift` (Save + chips), `AppState` (init load, erase step 2c) | build (iOS Sim); store mirrors the tested JournalStore pattern | implemented | (this branch) |
| UC-watch (A7.2 Area ⑧ deltas) | Watch glance honesty fixes on the phone sender: `stateLine` now mirrors the phone's ACTUAL affirming line (same three approved strings + same thresholds as `TodayView.affirmHeadline`; was hardcoded "steady" regardless of the user's week) and the live Recovery value carries its " ms" unit; complication `.accessoryInline` gets its own single-line branch. Watch app screens themselves MATCH the canvases (census) — no watch-side changes | `Liviqa/Connectivity/PhoneWatchSync.swift` (`watchStateLine(for:)` + unit fix), `LiviqaWatch/Complication/LiviqaComplication.swift` (inline branch) | build (iOS Sim + watchOS Sim, both schemes green) — RAILS: descriptive-only non-MDSW copy (the three lines are the phone's approved set), provenance never in the payload, no nudges on the wrist | implemented | (this branch) |
| FR-SET-03 (NEW — Settings states are observed, never asserted) | Every status Settings renders about the citizen's own data is DERIVED from the state the app can actually look up, and each genuinely-unknown case says so rather than defaulting to a reassuring word. Closes the 2026-08-13 TestFlight data-honesty incident on this surface: the three "My data" rows carried their statuses as string literals ("Apple Health · Connected", "Health Vault · 3 files", "Sundhedsplatformen · Not connected"), so they rendered identically for every citizen on every device — a tester was told an empty encrypted space held 3 files, and that Apple Health was connected while Home was still waiting for its first sample. Also in scope: the Settings connected-count now equals the count on the screen it pushes; the vault count is read with the SAME `HealthVaultSession.open` the vault screen uses; the version row reports the running build instead of the frozen literal "Version 1.0"; the profile subline names where the shown name came from instead of claiming "Device-stored only · never uploaded" over an account name; "Demo User"/"Demo session" stand-ins are gone; the consent-log line reuses the FR-WAL-09-gated ledger claim instead of asserting append-only on its own authority; the "no active sharing" card waits for the record to be read before claiming nothing is shared. WORDING RULE HELD (`AppState.HealthReadOutcome`): HealthKit cannot report READ authorisation, so no Apple Health status claims a denial — `.noReadings` says only that the read completed empty | `Liviqa/Views/SettingsView.swift` (`SettingsStatus` — pure resolvers for Apple Health / vault / Sundhed / account / name origin / version; `readVaultState()`; rows now push the screen their status describes), `Liviqa/Views/DataSourcesView.swift` (`connectedSourceCount` — the ONE shared counter, and `connectResultNote` — the one-tap connect now reports the outcome instead of always saying "Connected") | **T-SET-03 = `LiviqaTests/SettingsTruthTests.swift`** (15 tests, GREEN 2026-08-13): truth tables per resolver; `brandNewCitizenIsNotToldItIsConnected`; `anEmptyReadOutranksAStaleConnectedRow`; `noAppleHealthStatusEverClaimsADenial`; `vaultPrintsOnlyCountsItActuallyObtained` (no digit may appear in any non-`.ready` vault status); `connectedCountExcludesPlacesAndCountsSundhedOnce`; `connectResultReportsTheOutcomeNotTheHope`; `consentLogClaimIsTheGatedOne`; and the two SOURCE LINTS — `statusArgumentsAreNeverLiterals` (parses every `connectedSourceRow` call and fails if a `status:` argument is a string literal, including one hidden inside `String(localized:)` — negative-control verified by re-injecting `status: String(localized: "3 files")`, which fails the suite) and `eachStatusReadsItsRealSource` (each row must still read its named truth source) — RAILS: FR-ARCH-05 honest states; FR-ING-15 vault access states; FR-WAL-09 claim gating; provenance never renders (`scripts/guard_provenance.sh` green) | implemented (data-honesty incident batch, 2026-08-13) | (this branch) |

| FR-JRNL-SCOPE-01 (NEW — one journal per ACCOUNT, not per device) | The on-device journal is namespaced to the signed-in account: `<Application Support>/journal/<sha256(accountID)>/journal.v1.json`, the shape `EncryptedAnchorStore`/`LocalUserScope` already uses for the vault, except scoped by the ACCOUNT because the leak is between accounts on one device. Closes half of the 2026-08-13 TestFlight data-honesty incident: `journal.v1.json` was device-scoped and `signOut()` cleared only an in-memory array while `JournalView` held its own `@State` read straight from disk, so a new account on a used phone opened the previous person's journal. Now: no account id ⇒ no path ⇒ nothing readable and nothing written; a new account starts EMPTY; sign-out deletes nothing (the entries stay sealed in the departing account's own scope) and `JournalView` reloads to empty on the account change. MIGRATION (non-destructive): the pre-10.101 device file is ADOPTED into the first signed-in account's scope — copied, read back and counted at the new path BEFORE the old path is removed; an unreadable file is left byte-for-byte alone; entries whose `userId` names a different account are never adopted and the file is left for its owner; the marker is per-account so a decline never blocks the rightful account. Residual risk recorded in RISK (first-adopter ambiguity) | `Liviqa/Security/JournalStore.swift` (rewritten — `url(forAccount:)`, `openForAccount`, `migrateLegacyIfNeeded`, `eraseAccount`; no unscoped default URL exists any more), `Liviqa/Views/JournalView.swift` (`openJournal`/`persistJournal`, `.task(id: journalAccountID)`), `AppState.journalAccountID` + `signOut` + `deleteAllData` step 2 + `exportMyData`, `Liviqa/Views/DataSourcesView.swift` (the "add a note" path used to overwrite the whole file with one entry) | **T-JRNL-SCOPE-01 = `LiviqaTests/JournalScopeTests.swift`** (18 tests, GREEN 2026-08-13): `oneAccountCanNeverReadAnothersJournal`, `bothAccountsKeepTheirOwnEntries`, `noAccountIdMeansNoPathAndNoWrite`, `legacyDeviceFileIsAdoptedIntoTheSignedInAccountScope`, `afterAdoptionTheNextAccountStartsEmpty`, `migrationNeverOverwritesAJournalTheAccountAlreadyHas`, `migrationIsIdempotent`, `entriesAuthoredByAnotherAccountAreNotAdopted`, `anUnreadableLegacyFileIsLeftExactlyAsItIs`, `eraseRemovesOnlyThatAccountsJournal`, `signOutLeavesNothingAddressableAndDeletesNothing` | implemented (data-honesty incident batch, 2026-08-13) | (this branch) |
| FR-JRNL-SEED-01 (NEW — the demo seed can neither be written nor read) | The three `JournalView.demoSeed` entries a pre-gate June build wrote into `journal.v1.json` are purged, once and idempotently, by EXACT whole-body match against the app's own strings — a citizen who wrote their own words about a 6.2 fasting glucose keeps every one of them (prefixes, suffixes and quotes are all kept). The purge runs on read, on migration and on write, so the seed cannot be persisted in ANY configuration — that write path is how a DEBUG seed became "the citizen's own words". Seeding itself stays `#if DEBUG` AND is now further gated on a demo session (`isDemoData`), per the demo-seed rail. Nothing logs entry text | `Liviqa/Security/JournalStore.swift` (`legacyDemoSeedBodies` — a REMOVAL denylist; `isLegacyDemoSeed`, `purgingLegacyDemoSeeds`, filtering inside `save`), `Liviqa/Views/JournalView.swift` (seed block + its only reference inside `#if DEBUG`) | **T-JRNL-SEED-01 = `LiviqaTests/JournalSeedPostureTests.swift`** (7 tests) + the purge half of `JournalScopeTests` (`migrationDropsTheDemoSeedsAndKeepsEverythingElse`, `purgeMatchesWholeBodiesOnlyNeverFragments`, `purgeIgnoresSurroundingWhitespaceOnOurOwnSeed`, `purgeIsIdempotent`, `aDemoSeedCanNeverBeWrittenToDisk`, `seedsAlreadyOnDiskNeverReachTheReader`), all GREEN 2026-08-13. Posture lints: `theSeedIsOnlyEverCONSTRUCTEDInsideIfDEBUG` (walks every file in `Liviqa/`; a seed body outside `#if DEBUG` anywhere but the denylist fails), `theDenylistIsTheSeedVerbatim` (parses `JournalView.demoSeed`'s literals — editing the seed without the denylist fails), `everyDemoSeedReferenceIsDebugGated`, `theJournalStateStartsEmptyNotSeeded`, `theStoreNeverConstructsAJournalEntry`, `savingFiltersTheSeedInEveryConfiguration` (asserts the bytes on disk), `noDeviceScopedJournalPathRemains`. `ReleasePostureTests.journalAndVaultDemoSeedsAreDebugGated` strengthened from whole-line to every-line | implemented (data-honesty incident batch, 2026-08-13) | (this branch) |
| FR-ACC-NAME-01 (NEW — an identifier is never a name) | One pure function decides what the app may call a person: the name declared in onboarding (UC-01, `liviqa.profile.displayName`) wins; otherwise a backend `displayName` is used ONLY if it is a name — anything containing "@" is an address, and anything EQUAL to the local-part of the account's own email is a placeholder (provider-agnostic: Apple private relay is the observed case, not a special case); otherwise no name is known and the surfaces greet without one ("Good morning."). Closes the greeting half of the 2026-08-13 incident, where `/me` returned `75sg6pvfys` as `displayName` and the declared name only applied when that value was EMPTY. Resolved ONCE in `AppState`, so every consumer of `profile?.displayName` (Today greeting, Settings, avatar, profile sheet) is fixed at the source; `setDisplayName` also refuses to store an address | `Liviqa/Models/DisplayNameResolution.swift` (new — pure Foundation), `AppState.applyResolvedDisplayName` (called from `loadProfile` + `signInDemo`), `AppState.setDisplayName` | **T-ACC-NAME-01 = `LiviqaTests/DisplayNameResolutionTests.swift`** (13 tests, GREEN 2026-08-13): the four required cases (declared wins · local-part rejected · real backend name accepted · nothing known ⇒ nil) plus `anythingEmailShapedIsRefused`, `aNameThatMerelyResemblesTheMailboxIsStillAName`, `localPartExtraction`, and the AppState wiring (`appStateDropsAPrivateRelayPlaceholderFromTheProfile`, `appStateKeepsARealBackendName`, `appStateLetsTheDeclaredNameBeatThePlaceholder`, `appStateRefusesToStoreAnAddressAsTheDeclaredName`). The suite also caught a real exclusivity trap in the first implementation (`profile?.x = f(profile?.x)`) | implemented (data-honesty incident batch, 2026-08-13) | (this branch) |

### Donor programme DON-2026-01 — app side (2026-08-13, branch claude/a72-electric-ink)

_Basis: `Liviqa_DonorDataProgramme_v01_20260813.md` (§4.5 export-not-upload, §5 revocation, §6 the hard rule) + **CN decision 2026-08-13**, recorded verbatim: "forget about anonymity. We need to have a cloud version of the data. and a permit to use it for the training." That decision RESOLVES OD-D1 against the v01 recommendation (the corpus is for model training, not verification only) and abandons anonymity as a strategy — protection comes from lawful basis, explicit consent, encryption, access control and governance. Recorded as CN's decision, not as a silent widening. **The app side is unaffected by the training decision**: the app assembles and seals, it never trains, never uploads, and holds no corpus. The programme-doc consequences (§1.2(1) prohibition, §2.3 consent text, DPIA v02, ROPA) are OWNER/COUNSEL work and are NOT closed by these rows._

| Req ID | Title | Design element (file) | Test ID | Status | PR |
|---|---|---|---|---|---|
| FR-DON-01 [PROV — SRS v06 pending] | Donor programme constants + the DONOR GATE: the whole flow exists only in a build compiled with `-D LIVIQA_DONOR`; in every shipped binary `isDonorBuild` is a compile-time `false`, so no citizen surface, state or storage exists. Programme public key is deliberately UNSET (`publicKeyBase64 = nil`) until the §7.3 item-7 custodian ceremony; a DEBUG-only env override (`LIVIQA_DONATION_PUBKEY`) exists for donor-build QA and cannot exist in Release | `Liviqa/Donation/DonationProgramme.swift`, `Liviqa/Views/SettingsView.swift` (`donorSection`, gated) | **T-DON-01** = `DonationEgressTests.donorBuildFlagIsCompileTimeGated`, `.noProgrammeKeyMaterialIsCommitted`, `DonationConsentTests.aShippedBuildHasNoDonationStateAndCanRecordNothing` | implemented (GREEN 2026-08-13) | PR-112 |
| FR-DON-02 [PROV] | Donation payload + pure assembler: four streams only (glucose, daily heart, sleep segments, workouts); minimisation expressed as ABSENT FIELDS (no slot for labs, diagnoses, medication, journal/notes/chat, location, routes, BP, AFib, insulin, body composition); REAL-provenance rows only (a demo/mock/LV001 row cannot enter a corpus); HealthKit source names classified to a device class + salted discriminator so a user-named device ("Claus' Apple Watch") never leaves; per-donor whole-week date shift ±26 weeks with each row's ORIGINAL UTC offset carried; deterministic bytes | `Liviqa/Donation/DonationPayload.swift` | **T-DON-03** = `DonationPayloadTests` (14 tests): top-level key set, series leak scan, simulated/external dropped, source classification, shift is whole weeks + weekday/gap preserved, window + scope filters, manifest↔series agreement, no copy claims anonymity, byte determinism | implemented (GREEN) | PR-112 |
| FR-DON-03 [PROV] | Seal-on-device, one-way: X25519 ephemeral → HKDF-SHA256 (salt = ephemeral‖recipient public keys) → AES-256-GCM, header authenticated as AAD, envelope `LVDN1`. Same ECIES discipline as `KeyWrap`/`HealthVaultStore`, Curve25519 because the custodian half lives on hardware tokens, not in a Secure Enclave. **The app has no `open`** — no `AES.GCM.open`, no recipient private key, so a donor build cannot read back what it sealed. Plaintext exists only as an in-memory `Data` between assembly and sealing | `Liviqa/Donation/DonationSealer.swift` | **T-DON-04** = `DonationSealTests` (8 tests): round-trip under a throwaway custodian key, wrong key fails, tampered byte fails at 5 positions, no plaintext in the sealed bytes, fresh ephemeral per seal, digest stability, no open-side in the app target | implemented (GREEN) | PR-112 |
| FR-DON-04 [PROV] | Consent first, revocable, ONE consent model: the donation grant is a `WalletGrant` with the new `RecipientType.controllerInternal` (§2.6 / OD-D2), grant/withdraw write ordinary `WalletEvent` rows (`consentGranted`/`consentRevoked`), and every export writes a `dataAccessed` ledger row naming the streams. Grant + events + export log persist DEVICE-LOCAL, sealed with the KeyVault DEK (`CryptoBox`, atomic, `.completeFileProtection`) — an unreadable record is reported, never overwritten. Grant expiry 12 months, no auto-renew; expired or withdrawn ⇒ every export refuses. Erased by `deleteAllData` step 2c-iii | `Liviqa/Donation/DonationConsentStore.swift`, `Liviqa/Donation/AppState+Donation.swift`, `Liviqa/Models/WalletModels.swift` (`controllerInternal`), `Liviqa/AppState.swift` (storage + erase) | **T-DON-05** = `DonationConsentTests` (13 tests): gate truth table incl. withdrawn/expired/wrong-recipient/empty-scope, refusal ordering, sealed round-trip across a relaunch, wrong-key ⇒ `.unreadable`, nothing plaintext on disk, no parallel consent model, `controller_internal` never in a citizen picker | implemented (GREEN) | PR-112 |
| FR-DON-05 [PROV] | Donation is an **EXPORT, not an upload**: assemble → seal → write ONE sealed file (atomic, `.completeFileProtection`) → iOS share sheet. The app opens no connection; the donor performs the transfer to the programme's one-time URL. Donor screen states the file's exact contents (counts from the SAME assembler the export runs), what is absent, the date shift and the pseudonymity notice; staged files purged on export, on withdrawal and on demand | `Liviqa/Donation/DonationExport.swift`, `Liviqa/Views/DonorExportView.swift` | **T-DON-02** = `DonationEgressTests` (9 tests) + **blocking build step** `scripts/guard_donation_egress.sh` wired in `build-ios.sh` on the day it was written: no transport construct/endpoint/networking import anywhere in `Liviqa/Donation` + `DonorExportView`; the only `.write(to:)` is of the sealed bytes; the donation path never touches `contributeHealthResearch`/`ingestSundhed`; payload types are Encodable-ONLY and no decoder or importer exists (§6 hard rule); `DataProviderKind` stays `{healthKit, mock, lv001}` | implemented (GREEN; guard wired + verified passing) | PR-112 |
| FR-DON-06 [PROV] | The conditional copy fix (§6.3 / OD-D11): "Your individual readings never leave this phone." renders UNCHANGED, verbatim, for every citizen without an active donation grant — which in every shipped build is everyone. With an active donation grant the surface renders the donor sentence, which names the exception specifically (the app still sends nothing; the donor exports an encrypted copy themselves; withdrawal ends it). Single source `DonationCopy`; per-grant recipient rows describe THAT recipient, so a clinician's row is untouched for a donor | `Liviqa/Donation/DonationCopy.swift`, `Liviqa/Views/StudyConsentView.swift`, `ResearchHubView.swift`, `WalletView.swift`, `InAppPrivacyView.swift` | **T-DON-06** = `DonationConsentTests.theUnconditionalPromiseRendersForEveryNonDonor` (verbatim), `.theDonorVersionNamesTheExceptionSpecifically` (no weasel wording), `.recipientRowsAreScopedPerGrantNotPerDonor`, `.noSurfaceHardCodesTheReadingsClaim` (no view may re-hard-code the sentence), `.theConsentSurfacesCallTheScopedClaim` | implemented (GREEN) | PR-112 |

**Unweakened invariants (re-verified, not touched):** `T-RSCH-05` (contribution has exactly one call site, a Button action, aggregate payload) and `T-SUND-01` (the on-device sink is terminal; one `ingestSundhed` call site app-wide) are GREEN and unmodified — the export model exists precisely so neither had to be relaxed. Full suite 584 tests / 81 suites green.

**Known gap, stated rather than papered over:** §3.2 lists STEPS as a needed stream. The on-device store persists no steps entity (`IngestionCoordinator`: steps and active energy stay on the in-memory `HealthSamples`), so no donation can contain them and the donor screen says so. Adding them is a store change, out of scope for this PR and tracked for the programme owner.


## Upcoming (tracked, not yet implemented)

| Req ID | Title | Target PR |
|---|---|---|
| NFR-SEC-02 / OD-09 | Secure Enclave key-wrapping + AES-256 verification | `Liviqa/Security/CryptoCore.swift`, `Liviqa/Security/KeyVault.swift` | T-SEC-01..06 | implemented (10/10 checks pass; SE w/ software fallback) | PR-8 |
| FR-SHARE-02 | `LiviqaBackendService: SupabaseServiceProtocol` against the EU-sovereign backend (Ory auth) + `pushDerivedShare` → swap `AppState(supabase:)` behind `Config.backend` | next (Xcode session; see `docs/Sovereign_Backend_Integration_v01.md`) |
| NFR-SEC-07 / D-SOV | Demote Supabase to sandbox; real PII on Scaleway+Ory only (US-parented provider off the PII path) | with FR-SHARE-02 |
| OD-07 reconcile | Migrate locked `MetricSnapshot.glucoseMgdl` (journal sync + Supabase col) to mmol/L | later (touches sync schema) |
