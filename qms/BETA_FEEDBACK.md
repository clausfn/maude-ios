# TestFlight Beta Feedback Register

_Source: App Store Connect `betaFeedbackScreenshotSubmissions` (fetched via the ASC API key, app
`dev.liviqa.app` / 6776228205). One row per submission; FB ids are the ASC submission-id prefixes.
Status: fixed / partial / superseded / proposed / open. Last reconciled: 2026-08-24 (32 submissions)._

| FB id | Date | Build | Feedback (verbatim gist) | Diagnosis | Status |
|---|---|---|---|---|---|
| FB-AEkAWxal | 06-15 | 10.47 | "I tried to change text size and it did not change. The font is small and will collide with usability requirements" | **Dynamic Type not honored.** The `Theme.swift` font helpers used FIXED point sizes (`.system(size:)` / `.custom(_:size:)` with no `relativeTo:`), so the iOS text-size setting did nothing — and NFR-A11Y-01 had been marked "implemented" on the false belief that `Font.custom(_:size:)` auto-scales. Same root cause as FB-AN9QOlAh's "font is massive" half (opposite symptom — text was simply stuck at one fixed size). No in-app text-size slider exists; the tester means the system setting. | **FIXED** (PR-97, 2026-06-16, approved by CN): single central fix in `Theme.swift` — SF Pro helpers route through `UIFontMetrics`, mono helpers gain `relativeTo:`; all ~566 call-sites now scale, default look unchanged, the two intentional chrome clamps kept. Re-verified in the simulator at Large vs accessibility-extra-large (hero/body/kickers grow). Pending TestFlight ship. **Also closes FB-AN9QOlAh (a).** |
| FB-AJR9AqEk | 06-15 | 10.47 | "Moving from Home to Insights it will not respond. There is a delay and I have to push many times" | **Main-thread stall during the tab switch.** `AppState.refreshFromHealth()` ran the whole deriver chain (NudgeEngine, PatternEngine, CorrelationDeriver O(samples·days·signals), PassportStats/TodaySignals/Sleep) synchronously on the `@MainActor`, so tab taps fired during the refresh were dropped until it finished. Aggravated by a redundant per-appearance refresh `.task` on Home racing the launch refresh. | **FIXED** (PR-97, 2026-06-16, approved by CN): the pure `Sendable` deriver chain now runs in `Task.detached(.userInitiated)`; awaiting it suspends the main actor so taps register immediately, and only the `@Observable` assignments hop back to main. Removed the redundant Home `.task`. Build-green; the stall only reproduces on a real device (HealthKit path), where the block is now removed. Pending TestFlight ship + on-device confirm. |
| FB-AIJMHfz6 | 06-13 | 10.43 | "We need to add two consent boxes to this page: ☑ Allow me to be included in anonymous cohort discovery; ☑ Allow me and my data to be discovered for anonymous research — with some explanation as to what they allow" (Brian Connelly) | Screenshot is the **DfG onboarding intro** (`DfGOnboardingView`), not the in-app Privacy screen. Needs two opt-in (default OFF) consent toggles + one-line explanations, written to the consent ledger, revocable in Privacy. Same ask as FB-AGtO8N6h #2, re-raised by Brian against the onboarding screen. | **FIXED** (built 2026-06-15, approved by Claus): two opt-in (default OFF) consent boxes on the DfG onboarding (`DfGOnboardingView` Page 1) — "anonymous cohort discovery" + "discoverable for anonymous research", each with a one-line explanation; persisted via `@AppStorage` (`consentCohortDiscovery`, `consentResearchDiscoverable`), and **now also surfaced + revocable in the Privacy tab** (a "Research contributions" section with the two toggles, 2026-06-15). Build green; rendered in the simulator (unchecked + checked). Pending TestFlight ship. **Also closes FB-AGtO8N6h #2.** |
| FB-AN9QOlAh | 06-13 | 10.41 | "Font is massive and the graphs are not showing the new actual data set I told you to use with my latest upload — logging in as Claus" | Two issues: (a) text rendering oversized on at least one screen; (b) signed-in (Claus/LV001) account showed demo seeds, not the real dataset — root cause: graph data was local-HealthKit-or-MockData only; no account/upload path existed, the goldmine JSON was never bundled, and `LV001Provider` was an unwired stub | **(b) FIXED** (PR-87, 10.43): `LV001Dataset` composed from the consented goldmine (TIR 88%, GMI 6.8%, 3 499 days, HRV/RHR/steps/sleep + 7-day grid), loaded on LV001 login — verified on Home chips + "Your Week" TIR/HRV in the simulator. **(a) FIXED** (PR-97, 2026-06-16): the "font is massive" symptom was the same root cause as FB-AEkAWxal — fonts never honored Dynamic Type, so text was stuck at whatever the tester's system size rendered (massive for them, small for another). Central `Theme.swift` UIFontMetrics fix resolves both. **FB-AN9QOlAh fully closed.** |
| FB-AGtO8N6h | 06-13 | 10.40 | "1) Switch white DfG logo for multi-colour or all-blue. 2) Need the two discovery & can-be-contacted consent tick boxes. 3) Most explanation text needs rework (tooltip box?)" | (1) DfG onboarding mark was the white/negative variant on the cream `paper` canvas → invisible (breaks the locked logo rule: light bg → colour). (2)/(3) consent screen lacks the discovery + contactable toggles and the long copy needs progressive disclosure | **(1) FIXED** (PR-84, 10.41): onboarding uses the colour `dfg-logo` on light. **(2) FIXED** (2026-06-15, with FB-AIJMHfz6): the discovery + can-be-contacted consent boxes added to the DfG onboarding. **(3) FIXED** (2026-06-15): heavy onboarding paragraphs reworked into a tap-to-expand `ExpandableNote` (ⓘ summary + chevron, moss card) on Pages 1–2 — leads with one idea, tucks the detail a tap away. **FB-AGtO8N6h fully closed.** |
| FB-AIPKcW7v | 06-12 | 10.39 | "Care team not showing on citizen video call" | ConsultView only carried a generic "Care team" Jitsi tile — the citizen couldn't see who they were speaking with | **FIXED** (PR-85, 10.42): "In this call" card under the video — recipient role · organisation + live presence dot |
| FB-AGtO8N6h-logo · FB-AMVqGNwA | 06-11 | 10.3 | "Use inverted white logo + center the logo and the DATA for GOOD header (maybe make the header bigger)" | Onboarding logo + kicker not centered; header small | **FIXED** — logo + "DATA FOR GOOD" header centered; on dark (Mac/Midnight) the white mark reads; on light it's now the colour mark (PR-84) |
| FB-AOIWoD6l | 06-10 | 10.27 | "Not how public hospitals work — either side requests a planned video consultation; other side accepts or proposes another slot (30-min slots)" | Booking needs a REQUEST → ACCEPT/COUNTER workflow, not direct booking. Telecare North / GP video-system best practice incoming from CN | **PARTIAL** (Stage A built 2026-06-15): iOS "Request this time" now posts to the backend — `CareConnect.requestConsult` → `POST /appointments/request` (status proposed:citizen) + captures a fallback phone; accept/decline loop was already wired (`respondToProposal`). Build-ready spec written (`Liviqa_VideoConsult_Spec_v01`, grounded in Min Læge / Sundhedsplatformen-Epic / FHIR). Scoped by CN (no Telecare material needed). Built: pre-visit check-in + device test (PR-92), calendar deep-link + reminder ladder (PR-93), virtual waiting room + auto-launch (PR-94), **simulated free/busy availability + 15-min grace/reschedule** (PR-95), and the **clinician "running late" one-tap** on the console (`ConsultPage`, `e6bfd7a`). Counter-propose + moderator already exist (`respondToProposal` + clinician-starts-room). **Backend comms now SCAFFOLDED** (`liviqa-backend` `1ce1f16`, env-gated, no-op until provisioned): `IcsService` (RFC5545 VEVENT + VALARM ladder), `MailService` (Scaleway TEM via fetch), `ApnsService` (token .p8 via jose+http2), `ReminderService` (T-24h/1h/10m); booking-accept emails the `.ics` invite. **Switch on by setting env:** `TEM_API_KEY`+`TEM_PROJECT_ID`+`MAIL_FROM`; `APNS_KEY_PATH`+`APNS_KEY_ID`+`APNS_TEAM_ID`; `REMINDERS_ENABLED=1`. **Remaining:** Claus provisions Scaleway TEM + Apple APNs key; iOS device-token registration (so push can target a phone); and the live EMR/HIS SMART-Slot feed (availability is device-simulated until then). |
| FB-AIEzHyog | 06-09 | 10.10 | "Flow is wrong — inform/call user, then plan in calendar; clinician should be default moderator; multi-party for specialists; planned + instant flows" | Same workflow theme + moderator default | **PARTIAL** — instant flow now auto-notifies the citizen in-app (PR-68); planned flow = the request/accept workflow above; moderator default = console starts the room (is moderator) |
| FB-ACOOcPPf | 06-09 | 10.18 | "What is the use case vs ŌURA? A bit restrictive" | Positioning question, not a defect — Liviqa's difference is consent/sharing/care, not ring metrics. Also the assistant deflects cross-metric questions to canned answers | **OPEN (narrative + product)** — onboarding/pitch copy; assistant cross-metric capability is a separate product decision |
| FB-AFf8FjC1 | 06-09 | 10.18 | "This view of consent is broken" | Privacy grant cards rendered raw metric keys through a stub FlexHStack (plain HStack) → chips crushed into vertical letter-shreds | **FIXED** (PR-69): real FlowLayout + grouped, deduped chips ("Glucose", "Activity"…) |
| FB-AEZv4wJ8 | 06-09 | 10.16 | "Not working with the calendar" | Old (pre-redesign) journal calendar strip overlap; PlanConsult was local-only | **SUPERSEDED** — scheduling is now backend-real; verify on 10.42 |
| FB-ABpGIUrM | 06-09 | 10.16 | "Same" | Same area, same minute as above | **SUPERSEDED** (with AEZv4wJ8) |
| FB-AJ8M7416 | 06-09 | 10.16 | "This will not work ;)" | Same scheduling area | **SUPERSEDED** — replaced by real booking + (proposed) request flow |
| FB-AK0022Mv | 06-09 | 10.15 | "Can we hide the technical token?" | Consult pre-join showed the raw Jitsi room id ("Liviqa Consult Cmq 67 Ttke…") | **FIXED** (PR-68): call titled "Liviqa video call", room id never shown |
| FB-AOUGIncO | 06-09 | 10.10 | "Confusing start on the mobile screen on video" | Jitsi welcome/prejoin noise + full toolbar | **FIXED** (PR-68): prejoin off, toolbar = mic/camera/hangup |
| FB-APgHLoge | 06-09 | 10.10 | "How do I navigate here? Where is the LV icon — whole navigation is not intuitive" | Old-build navigation (pre-v2 tabs) | **SUPERSEDED** — v2 tab bar + app-bar avatar; verify on 10.42 |
| FB-AH-UhwJB | 06-09 | 10.10 | "My LV round icon is gone — how do I navigate here" | Old-build navigation (pre-v2 tabs) | **SUPERSEDED** (with APgHLoge) |
| FB-AD-6ueLk | 06-09 | 10.10 | "Classic chat issue — can't see the message" | Old-build care-messaging message visibility | **SUPERSEDED** — v2 messaging surface; verify on 10.42 |
| FB-AKqbKvLm | 06-09 | 10.10 | "Unreadable — and DfG tokens should be using the DfG logo" | Token-wallet cost badges were low-contrast (light text on light pill); token card lacked the DfG mark | **FIXED** — cost badges now use the inverted bg/fg pair (readable in both themes); DfG colour/negative mark on the token card; verify on 10.42 |
| FB-ADZ4J5fm | 06-04 | 10.2 | "The graphic for Time in Range does not work" | Early Trends chart render issue | **SUPERSEDED** — v2 Trends (TIR line + 7-day context grid); verify on 10.42 |
| FB-AJpKzCR8 | 06-04 | 10.2 | "Same" | Same TIR chart | **SUPERSEDED** (with ADZ4J5fm) |
| FB-AA0v59K9 | 06-04 | 10.2 | "Check colour on buttons" | Pre-v2 button palette | **SUPERSEDED** — v2 palette (moss/clay) |
| FB-AH9WzbRF | 08-15 | 10.101 | "Good wrap of my day" (Christel Friis Conrad) | **Praise, not a defect** — submitted against the post-21:00 evening Home (Closing Note "Today held steady."); the evening edition is landing as designed. The actionable finding is in the attached screenshot: a **Danish-locale device renders the content column in English** while the tab bar/date are Danish. `Localizable.xcstrings` = 754 keys, 656 with `da` → **98 missing**, clustered on the newest feature (the whole Sundhed.dk / health-record set: "Bring in your Sundhed.dk records", "Your health record", "Lab results (%lld)", "Diagnoses (%lld)", …). Separately the evening-edition strings (`Good evening.`, `Closing note`, `Today held steady.`, `TodayView.swift:1080–1114`) are wrapped in `String(localized:)` but **absent from the catalog** — never extracted, so they cannot resolve to Danish. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cbtp)). Proposed: (1) extraction build to land the evening keys, (2) translate the 98 missing `da` entries (Sundhed.dk cluster + evening edition first), (3) CI guard failing on <100% `da` coverage. |
| FB-AIsHH-fw | 08-17 | 10.101 | "This curve  I dont understand" (Christel Friis Conrad) | **The curve is not her data.** Under the headline "Din seneste hvilepuls er 71 bpm", the YOUR TREND card plots a hard-coded literal series — `AreaTrendChart(values: [9.1, 8.2, 7.6, 6.9, 6.4, 6.0])`, `NudgeDetailView.swift:265-271` — with no unit and no date axis (hence the 9/7/6 y-labels against a 71 bpm headline). The call site (`:146-154`) falls back to it for **every** nudge lacking a `chartPercent`, so invented data is labelled "YOUR TREND" above a real measurement, in a shipped build. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7ceu0)). Proposed: delete `trendFallback` (no real series ⇒ no chart; the honest `stillLearning`/`BaselineProgressBar` path already exists), plot real series with unit + date axis, and grep for other literal chart arrays reachable in a shipped build. **Raised to high priority despite the older build — trust defect, still present on 10.105.** |
| FB-AJYOrjNN | 08-17 | 10.101 | "Too much information and not clear what it says" (Christel Friis Conrad) | Resting-HR insight detail (`NudgeDetailView`) stacks **eight blocks** — kicker, headline, See why, YOUR TREND chart, "Se dataene bag dette", "Why you're seeing this", a disabled "Open — SNART" stub (`:195-201`), "Handl på dette øjeblik", the share-via-wallet card, "Drøft i assistenten" — around one fact ("71 bpm"). Two blocks are non-functional; the chart is fabricated (FB-AIsHH-fw); EN/DA mixed in one column. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cetz)). Proposed: default view = headline → one real chart → one primary action; move evidence/why/"Se dataene" behind the existing `showDepth` toggle; move the share card out of the default scroll; remove the SOON stub from the reading path. |
| FB-AONJFmG8 | 08-17 | 10.101 | "This is not relevant to me" (Christel Friis Conrad) | The **Glucose detail empty state** ("No glucose readings yet…"). The empty copy is honest; the defect is that a tester with no CGM was routed there from a Home tile one minute earlier (FB-ACCHrY3x). Page is also a dead end — no action, large blank area. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cety)). Proposed: remove the entry point (see FB-ACCHrY3x, same PR); add a "Connect a CGM or meter in Apple Health" action + tighten layout for anyone who still reaches the page. |
| FB-ACCHrY3x | 08-17 | 10.101 | "Clukose is not relevant to me and should not be shown" (Christel Friis Conrad) | Home grid rendered **Glukose · "Stabil" · "— in range"** with a Zones & TIR chip for a person with **zero glucose readings** — while the detail page said "No glucose readings yet". Two gaps: (1) `gridCards` falls back to `HomeCard.classicFour`, which includes glucose (`TodaySignalsDeriver.swift:34-39`), bypassing the FR-TOD-07 rule that `calibratingCards` (`:234`) encodes; (2) `glucoseVerdict` (`TodayView.swift:1087-1091`) guards only `coldStart`, so `signals == nil` nil-coalesces to `false` and returns **"Steady"** — a verdict computed from nothing. `sleepVerdict` guards `signals == nil`; glucose/recovery/heart do not. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cetx)). Proposed: no glucose card without a real reading under any seed/fallback path; guard the three verdicts like `sleepVerdict`; test zero-glucose ⇒ no card. Closes with FB-AONJFmG8. |
| FB-AEtkmZBO | 08-17 | 10.101 | "A lighter day - does not really make sense" (Christel Friis Conrad) | Evening Home, HOW TODAY SCORED: headline "A lighter day — they happen." over a 56 ring whose only components are **Søvn 36/50** and **Restitution 20/20** — recovery is perfect, sleep carries the whole deficit, and "lighter" names neither (reads as *lighter activity*). Three tones in one viewport: "Today added to a good week" / "Sleep unchanged" / "A lighter day". | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cetv)). Proposed: headline names the driver ("Sleep carried today's score"), drop "they happen", reconcile against the Since-last-week strip. `DayScoreComposer.swift` + its tests. |
| FB-ACqEguOk | 08-19 | 10.105 | "Headline under søvn does not make sense here \"a little longer\"" (Christel Friis Conrad) | **Correctness bug, not wording.** Home Søvn tile read **"A little longer / 4h 32"** while the Since-last-week strip on the same screen read **"↓ Sleep −110 min"**. `sleepVerdict` (`TodayView.swift:1084-1085`) keys off `sleepImproving` (`:651-654`), a *within-week* slope over `sleepWeek`, which can be true while the week-over-week delta is negative and the value sits below the own-usual band — two verdicts off two yardsticks, ten points apart on screen. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cett)). Proposed: derive the tile verdict from the same delta the strip reports; never say "longer" below the own-usual band; regression test. Check glucose/recovery/heart verdicts for the same shape. |
| FB-AJeSGf-j | 08-19 | 10.105 | "Good info and oversight but some wording difficult to understand- for example load - what does that mean: burned/used calories??" (Christel Friis Conrad) | Fitness detail workout rows end in a bare number over **load** ("163 load"). The Training-load card *does* define it ("Load points add up how long and how hard you moved…") but sits far above, and says "load points" where the row says "load" — so the row is read as calories. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cetp)). Proposed: row unit → "load points"; one line explicitly negating calories under the workout list; See-why decomposition on the rows. |
| FB-AFJ6gTV9 | 08-19 | 10.105 | "Fitness VO2max - please explain - dont understand it" (Christel Friis Conrad) | FITNESS · VO₂MAX card gives a headline, an `mL/kg·min` chart and a "your usual" band, but never says what VO₂max is, what the unit means, or why flat is fine. The Heart-rate-zones card directly above it *does* carry exactly such a footnote — the VO₂max card has no equivalent and no See-why chip. | **OPEN** — awaiting CN approval ([ClickUp](https://app.clickup.com/t/z8nrz7cetm)). Proposed: plain-language footnote in the zones-card voice, `SeeWhyChip` for FR-XPL-01 parity, unit spelled out once. |
| FB-AEX4IDQe | 08-19 | 10.105 | "Good info and oversight" (Christel Friis Conrad) | **Praise, not a defect** — the Activity · This week detail (hero "6 days above your usual line", 41% more steps, 102,948 steps / 6,004 kcal / 3 workouts, steps bars against the "your usual 10.4k" line). The own-baseline framing is landing as designed; it is the reference pattern for the cards that are *not* landing (FB-AJeSGf-j, FB-AFJ6gTV9). | **OPEN** — no build work ([ClickUp](https://app.clickup.com/t/z8nrz7cetq)). Record and close, unless CN wants the hero/stat-tile/own-usual-line pattern written up as the house template. |
| FB-APC4qJBj | 08-24 | 10.105 | "Try to make yellow and red zones for better visualisation. You did before" (Dev loper) | The **Day Replay ("Din dag") glucose card** draws the day over ONE pale in-range rectangle — `DayReplayChart` hard-codes `LiviqaTheme.fjordBright.opacity(0.07)` (`TrendsCharts.swift:~275`) and never reads `clinicalTIRZones`. The approved five-zone clinical ramp (PR-99, colour-safety frozen PR-105, CN 2026-08-12: tirVeryLow plum+hatch / tirLow / tirTarget / tirHigh amber / tirVeryHigh sienna+dots, every band text-labelled) already exists in `GlucoseCurveView.clinicalZones` (`OuraComponents.swift:168-194`) and is ON by default — so Glucose detail and Metric detail band the curve while Day Replay does not. The tester is right that "you did before": the feature ships, just not on this screen. | **FIXED** (PR-118, 2026-08-24, approved by CN; [ClickUp](https://app.clickup.com/t/z8nrz7cjkf)). The ramp was EXTRACTED, not duplicated: `ClinicalTIRZones` (`OuraComponents.swift`) is now the one implementation and `GlucoseCurveView` calls it — geometry proven identical to the pre-refactor offset formula to 0.001 pt across four axis/range configurations (`ClinicalTIRZoneGeometryTests`), so Glucose and Metric detail did not move. `DayReplayChart` follows `clinicalTIRZones`: on ⇒ the shared five-zone ramp; off ⇒ the previous single `fjordBright.opacity(0.07)` band, with the legacy y-domain reproduced to the value. Domain widens to at least 2.0–14.0 with zones on while still containing the day's own extremes, so HIGH is a readable stripe rather than a sliver. Stale "no red — personal-band framing only" comments corrected in `DayTimelineView.swift` and the `TrendsCharts.swift` header to state the real rule (glucose charts carry the ramp; RK-ALARM-01 exception; PR-105 non-colour signals). No new colour — the five signed-off tokens only. **Verified on the simulator in both flag states** (screenshots attached to the ClickUp task). RTM FR-VIZ-06 · RISK RK-ALARM-01 extended · T-VIZ-06. **Caveat recorded:** VERY HIGH is a hairline on a day that never exceeds 13.9 mmol/L — correct (nothing is up there) and identical to the two charts that already shipped. |

## Open / actionable now

1. ~~**FB-AN9QOlAh (10.41)** — font half.~~ ✅ **DONE** (PR-97): both halves closed — dataset (10.43) + the font/Dynamic Type root cause (same as FB-AEkAWxal).
2. ~~**FB-AGtO8N6h #2/#3 (10.40)** — consent toggles + tooltip copy.~~ ✅ **DONE** (2026-06-15): #2 consent boxes (with FB-AIJMHfz6) + #3 `ExpandableNote` progressive disclosure. Fully closed.
3. **FB-AOIWoD6l / FB-AIEzHyog** — video-consultation request→accept/counter flow + moderator model. *Awaiting CN's Telecare North material.*
4. **FB-ACOOcPPf** — assistant cross-metric capability (vs Ōura) + positioning copy. *Product decision.*

## Verify-on-next-device-pass (believed fixed/superseded, not yet re-confirmed on 10.42)

FB-AEZv4wJ8, FB-ABpGIUrM, FB-AJ8M7416, FB-APgHLoge, FB-AH-UhwJB, FB-AD-6ueLk, FB-AKqbKvLm, FB-ADZ4J5fm, FB-AJpKzCR8, FB-AA0v59K9.
